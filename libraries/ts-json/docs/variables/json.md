[Home](../README.md) > json

# Variable: json

A simple validating JsonConverter | JSON converter. Converts unknown to
JSON or fails if the unknown contains any invalid JSON values.

## Type

`JsonConverter`
