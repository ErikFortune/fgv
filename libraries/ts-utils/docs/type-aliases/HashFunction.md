[Home](../README.md) > HashFunction

# Type Alias: HashFunction

Function to compute a hash from a pre-normalized array of strings.

## Type

```typescript
type HashFunction = (parts: string[]) => string
```
