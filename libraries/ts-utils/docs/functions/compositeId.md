[Home](../README.md) > compositeId

# Function: compositeId

Helper function to create a Validation.Validator | Validator which validates a
strongly-typed composite ID.

## Signature

```typescript
function compositeId(params: CompositeIdValidatorConstructorParams<T, TCOLLECTIONID, TITEMID, TC>): CompositeIdValidator<T, TCOLLECTIONID, TITEMID, TC>
```
