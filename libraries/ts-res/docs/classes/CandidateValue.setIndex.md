[Home](../README.md) > [CandidateValue](./CandidateValue.md) > setIndex

## CandidateValue.setIndex() method

Sets the index of this candidate value.

**Signature:**

```typescript
setIndex(index: number): Result<CandidateValueIndex>;
```

**Parameters:**

<table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>
<tbody>
<tr><td>index</td><td>number</td><td>The index to set.</td></tr>
</tbody></table>

**Returns:**

Result&lt;[CandidateValueIndex](../type-aliases/CandidateValueIndex.md)&gt;

`Success` with the index if successful, or `Failure` with an error message if not.
