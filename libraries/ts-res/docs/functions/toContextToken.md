[Home](../README.md) > toContextToken

# Function: toContextToken

Converts a string to a ContextToken if it is a valid context token.

## Signature

```typescript
function toContextToken(token: string): Result<ContextToken>
```
