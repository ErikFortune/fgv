[Home](../../README.md) > [Convert](../README.md) > typedConditionDecl

# Function: typedConditionDecl

Returns a `Converter` for an `IConditionDecl<TQualifierNames>` narrowed on a supplied
`qualifierName` converter.

## Signature

```typescript
function typedConditionDecl(qualifierNameConverter: Converter<TQualifierNames>): Converter<IConditionDecl<TQualifierNames>>
```
