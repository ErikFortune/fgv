[Home](../../README.md) > [Convert](../README.md) > conditionKey

# Variable: conditionKey

`Converter` which validates an `unknown` value as a ConditionKey | ConditionKey.

## Type

`Converter<ConditionKey, unknown>`
