[Home](../../README.md) > [Helpers](../README.md) > createFillingVersionId

# Function: createFillingVersionId

Creates a composite FillingVersionId from filling ID and version spec

## Signature

```typescript
function createFillingVersionId(fillingId: FillingId, versionSpec: FillingVersionSpec): FillingVersionId
```
