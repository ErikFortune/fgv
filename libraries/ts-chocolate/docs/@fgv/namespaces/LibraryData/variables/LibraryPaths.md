[**@fgv/ts-chocolate**](../../../../README.md)

***

[@fgv/ts-chocolate](../../../../README.md) / [LibraryData](../README.md) / LibraryPaths

# Variable: LibraryPaths

> `const` **LibraryPaths**: `object`

Defined in: [ts-chocolate/src/packlets/library-data/navigation.ts:28](https://github.com/ErikFortune/fgv/blob/f0affaa177ad091881f5199fd53d6bab72652f4f/libraries/ts-chocolate/src/packlets/library-data/navigation.ts#L28)

Canonical paths within a chocolate library tree.

## Type Declaration

### confections

> `readonly` **confections**: `"data/confections"` = `'data/confections'`

Path to the confections data directory.

### fillings

> `readonly` **fillings**: `"data/fillings"` = `'data/fillings'`

Path to the fillings data directory.

### ingredientInventory

> `readonly` **ingredientInventory**: `"data/ingredient-inventory"` = `'data/ingredient-inventory'`

Path to the ingredient inventory data directory (user library).

### ingredients

> `readonly` **ingredients**: `"data/ingredients"` = `'data/ingredients'`

Path to the ingredients data directory.

### journals

> `readonly` **journals**: `"data/journals"` = `'data/journals'`

Path to the journals data directory.

### keyStore

> `readonly` **keyStore**: `"keystore.json"` = `'keystore.json'`

Default filename for key store (within user library root).

### moldInventory

> `readonly` **moldInventory**: `"data/mold-inventory"` = `'data/mold-inventory'`

Path to the mold inventory data directory (user library).

### molds

> `readonly` **molds**: `"data/molds"` = `'data/molds'`

Path to the molds data directory.

### procedures

> `readonly` **procedures**: `"data/procedures"` = `'data/procedures'`

Path to the procedures data directory.

### sessions

> `readonly` **sessions**: `"data/sessions"` = `'data/sessions'`

Path to the sessions data directory (user library).

### settings

> `readonly` **settings**: `"data/settings"` = `'data/settings'`

Path to the settings directory.

### settingsCommon

> `readonly` **settingsCommon**: `"common.json"` = `'common.json'`

Filename for common settings (within settings directory).

### settingsDevicePrefix

> `readonly` **settingsDevicePrefix**: `"device-"` = `'device-'`

Filename prefix for device-specific settings (within settings directory).
Full filename is `device-{deviceId}.json`.

### tasks

> `readonly` **tasks**: `"data/tasks"` = `'data/tasks'`

Path to the tasks data directory.
