[Home](../README.md) > atMost

# Function: atMost

Creates a filter for maximum value (inclusive).

## Signature

```typescript
function atMost(max: number, getter: (item: T) => number | undefined): FilterPredicate<T>
```
