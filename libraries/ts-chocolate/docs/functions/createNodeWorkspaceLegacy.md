[Home](../README.md) > createNodeWorkspaceLegacy

# Function: createNodeWorkspaceLegacy

Creates a workspace with Node.js platform defaults (legacy API).
Uses nodeCryptoProvider for key store and encryption operations.

## Signature

```typescript
function createNodeWorkspaceLegacy(params: IWorkspaceFactoryParams): Result<Workspace>
```
