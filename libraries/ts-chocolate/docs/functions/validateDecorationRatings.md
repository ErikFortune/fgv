[Home](../README.md) > validateDecorationRatings

# Function: validateDecorationRatings

Validate that ratings have valid scores (1-5), categories, and no duplicates.

## Signature

```typescript
function validateDecorationRatings(entity: IDecorationEntity): Result<true>
```
