[Home](../../README.md) > [Converters](../README.md) > sessionId

# Variable: sessionId

Converter for SessionId | SessionId (composite string).
Accepts either a SessionId | SessionId string or a `CompositeId` object representation.

## Type

`Converter<SessionId>`
