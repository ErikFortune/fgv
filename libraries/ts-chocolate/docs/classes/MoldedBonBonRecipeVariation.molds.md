[Home](../README.md) > [MoldedBonBonRecipeVariation](./MoldedBonBonRecipeVariation.md) > molds

## MoldedBonBonRecipeVariation.molds property

Resolved molds with preferred selection (lazy-loaded).
Returns empty options if no molds are configured.

**Signature:**

```typescript
readonly molds: IOptionsWithPreferred<IResolvedConfectionMoldRef, MoldId>;
```
