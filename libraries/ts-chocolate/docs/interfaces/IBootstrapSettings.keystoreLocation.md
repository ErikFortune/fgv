[Home](../README.md) > [IBootstrapSettings](./IBootstrapSettings.md) > keystoreLocation

## IBootstrapSettings.keystoreLocation property

Where to find the keystore file.
Defaults to local: `keystore.json` in user library root.

**Signature:**

```typescript
readonly keystoreLocation: ISettingsFileLocation;
```
