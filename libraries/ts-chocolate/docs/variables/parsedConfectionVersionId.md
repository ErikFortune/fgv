[Home](../README.md) > parsedConfectionVersionId

# Variable: parsedConfectionVersionId

Converter that parses a ConfectionVersionId | ConfectionVersionId string into its component parts
or validates a `CompositeId` object representation.

## Type

`Converter<ParsedConfectionVersionId>`
