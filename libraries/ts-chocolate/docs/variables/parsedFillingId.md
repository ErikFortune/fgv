[Home](../README.md) > parsedFillingId

# Variable: parsedFillingId

Converter that parses a FillingId | FillingId string into its component parts
or validates a `CompositeId` object representation.

## Type

`Converter<ParsedFillingId>`
