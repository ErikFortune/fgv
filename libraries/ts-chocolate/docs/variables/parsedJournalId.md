[Home](../README.md) > parsedJournalId

# Variable: parsedJournalId

Converter that parses a JournalId | JournalId string into its component parts
or validates a `CompositeId` object representation.

## Type

`Converter<ParsedJournalId>`
