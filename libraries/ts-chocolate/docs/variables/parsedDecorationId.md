[Home](../README.md) > parsedDecorationId

# Variable: parsedDecorationId

Converter that parses a DecorationId | DecorationId string into its component parts
or validates a `CompositeId` object representation.

## Type

`Converter<ParsedDecorationId>`
