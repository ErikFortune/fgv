[Home](../README.md) > EncryptedCollectionFile

# Type Alias: EncryptedCollectionFile

Encrypted collection file format - an encrypted file with collection-specific metadata.

## Type

```typescript
type EncryptedCollectionFile = CryptoUtils.IEncryptedFile<IEncryptedCollectionMetadata>
```
