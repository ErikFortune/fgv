[Home](../README.md) > MergeConflictStrategy

# Type Alias: MergeConflictStrategy

Conflict resolution strategy for merging collections with duplicate base IDs.

## Type

```typescript
type MergeConflictStrategy = "skip" | "overwrite" | "rename"
```
