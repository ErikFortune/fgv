[Home](../../README.md) > [Entities](../README.md) > [SessionLibrary](./SessionLibrary.md) > hasSession

## SessionLibrary.hasSession() method

Checks if a session with the given ID exists (searches all collections)

**Signature:**

```typescript
hasSession(sessionId: SessionId): boolean;
```

**Parameters:**

<table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>
<tbody>
<tr><td>sessionId</td><td>SessionId</td><td>The session ID to check</td></tr>
</tbody></table>

**Returns:**

boolean

`true` if the session exists
