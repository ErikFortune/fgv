[Home](../README.md) > [IKillerConstraints](./IKillerConstraints.md) > excludedNumbers

## IKillerConstraints.excludedNumbers property

Numbers that cannot be present in the combination.
Must be unique values between 1-9.

**Signature:**

```typescript
readonly excludedNumbers: readonly number[];
```
