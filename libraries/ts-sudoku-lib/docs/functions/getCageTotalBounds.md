[Home](../README.md) > getCageTotalBounds

# Function: getCageTotalBounds

Calculate the minimum and maximum possible totals for a cage of given size

## Signature

```typescript
function getCageTotalBounds(cageSize: number, maxValue: number): { min: number; max: number }
```
