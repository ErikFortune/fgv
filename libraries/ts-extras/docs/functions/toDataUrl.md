[Home](../README.md) > toDataUrl

# Function: toDataUrl

Formats an IAiImageData as a `data:` URL suitable for browser display.

## Signature

```typescript
function toDataUrl(image: IAiImageData): string
```
