[Home](../../README.md) > [CryptoUtils](../README.md) > [ICryptoProvider](./ICryptoProvider.md) > verify

## ICryptoProvider.verify() method

Verifies a signature produced by ICryptoProvider.sign.
Delegates to `crypto.subtle.verify`; the algorithm is derived from
`publicKey.algorithm.name` — ECDSA keys are augmented with
`hash: 'SHA-256'`; all other algorithm names are passed through as-is.
Intended for Ed25519 and ECDSA-P256 asymmetric public keys; for
HMAC-SHA256 verification use ICryptoProvider.verifyHmacSha256 instead.

**Signature:**

```typescript
verify(publicKey: CryptoKey, signature: Uint8Array, data: Uint8Array): Promise<Result<boolean>>;
```

**Parameters:**

<table><thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>
<tbody>
<tr><td>publicKey</td><td>CryptoKey</td><td>A `CryptoKey` with `'verify'` usage (e.g. the public
half of a keypair generated by
CryptoUtils.ICryptoProvider.generateKeyPair | generateKeyPair with
`'ecdsa-p256'` or `'ed25519'`).</td></tr>
<tr><td>signature</td><td>Uint8Array</td><td>The raw signature bytes produced by `sign`.</td></tr>
<tr><td>data</td><td>Uint8Array</td><td>The original data that was signed.</td></tr>
</tbody></table>

**Returns:**

Promise&lt;Result&lt;boolean&gt;&gt;

`Success` with `true` if the signature is valid, `false` if it is
not, or `Failure` with error context if the operation itself failed.
