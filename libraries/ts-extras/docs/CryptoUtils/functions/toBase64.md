[Home](../../README.md) > [CryptoUtils](../README.md) > toBase64

# Function: toBase64

Encodes a `Uint8Array` to a base64 string.

## Signature

```typescript
function toBase64(bytes: Uint8Array): string
```
