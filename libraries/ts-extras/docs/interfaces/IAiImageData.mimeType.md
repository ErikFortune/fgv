[Home](../README.md) > [IAiImageData](./IAiImageData.md) > mimeType

## IAiImageData.mimeType property

MIME type, e.g. `'image/png'`, `'image/jpeg'`, `'image/webp'`.

**Signature:**

```typescript
readonly mimeType: string;
```
