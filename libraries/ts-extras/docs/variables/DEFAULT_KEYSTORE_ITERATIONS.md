[Home](../README.md) > DEFAULT_KEYSTORE_ITERATIONS

# Variable: DEFAULT_KEYSTORE_ITERATIONS

Default PBKDF2 iterations for key store encryption.
Higher than regular files since this protects the master key vault.

## Type

`number`
