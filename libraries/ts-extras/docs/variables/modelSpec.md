[Home](../README.md) > modelSpec

# Variable: modelSpec

Recursive converter for ModelSpec.
Accepts a string or an object whose values are themselves ModelSpec values,
with keys constrained to known ModelSpecKey values.
Uses the `self` parameter from `Converters.generic` for recursion.

## Type

`Converter<ModelSpec>`
