[Home](../README.md) > DEFAULT_SECRET_ITERATIONS

# Variable: DEFAULT_SECRET_ITERATIONS

Default PBKDF2 iterations for secret-level key derivation.
Lower than keystore encryption since these are used more frequently.

## Type

`number`
