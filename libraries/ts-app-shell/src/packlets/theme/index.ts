/**
 * Theme provider and hook for runtime theme switching.
 * @packageDocumentation
 */

export {
  ThemeProvider,
  useTheme,
  type ThemeId,
  type IThemeOption,
  type IThemeContext,
  type IThemeProviderProps
} from './ThemeProvider';
