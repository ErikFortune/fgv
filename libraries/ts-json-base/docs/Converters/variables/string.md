[Home](../../README.md) > [Converters](../README.md) > string

# Variable: string

A `StringConverter` which converts `unknown` to a `string`.
Accepts `IJsonConverterContext` but ignores it.

## Type

`StringConverter<string, IJsonConverterContext>`
