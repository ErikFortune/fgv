[Home](../README.md) > Validator

# Type Alias: Validator

A validator which validates a supplied `unknown` value to a valid JsonCompatibleType | JsonCompatible value.

## Type

```typescript
type Validator = BaseValidator<JsonCompatibleType<T>, TC>
```
