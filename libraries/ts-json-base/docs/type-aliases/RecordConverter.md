[Home](../README.md) > RecordConverter

# Type Alias: RecordConverter

A converter which converts a supplied `unknown` value to a valid record of JsonCompatibleType | JsonCompatible values.

## Type

```typescript
type RecordConverter = BaseConverter<Record<TK, JsonCompatibleType<T>>, TC>
```
