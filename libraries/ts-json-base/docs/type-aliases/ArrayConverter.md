[Home](../README.md) > ArrayConverter

# Type Alias: ArrayConverter

A converter which converts a supplied `unknown` value to a valid array of JsonCompatibleType | JsonCompatible values.

## Type

```typescript
type ArrayConverter = BaseConverter<JsonCompatibleType<T>[], TC>
```
