[Home](../README.md) > ObjectValidator

# Type Alias: ObjectValidator

A validator which validates a supplied `unknown` value to a valid JsonCompatibleType | JsonCompatible value.

## Type

```typescript
type ObjectValidator = Validation.Classes.ObjectValidator<JsonCompatibleType<T>, TC>
```
