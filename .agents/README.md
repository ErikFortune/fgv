# Codex adapters

`AGENTS.md` is the Codex entry point. `.agents/skills` exposes the canonical
workflows in `.claude/skills/`; roles stay canonical in `.claude/agents/` and are
mapped by `AGENTS.md`. Keep substantive behavior in one place to prevent drift.
