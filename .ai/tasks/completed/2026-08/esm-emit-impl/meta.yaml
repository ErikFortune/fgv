id: esm-emit-impl
status: shipped
packages: ['@fgv/ts-bcp47', '@fgv/ts-web-extras-webauthn', '(common/scripts)']
prs: []
closed: 2026-08-09
summary:
  intended: >
    Implement the ESM emit design.
  shipped: >
    The implementation, plus the finding that the design's central recommendation does not work: the `dist` ESM emit contains extensionless directory imports, which is why Node could not load it.
  diverged: >
    Reversed the design's verdict. Its own PR #603 did not merge under that number — the content reached `release` inside #607 (`71787e798`) together with the browser and tarball gates.
  sourceLine: ''
keywords: ['esm emit', 'design reversal', 'extensionless imports', 'bundler assumption']
sourceHash: 65e42f56fe0710fc
