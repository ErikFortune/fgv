# Stream State: ts-prompt-assist

**Status:** 🟡 phase A merged; B-1b foundation hardening merged; B-2 resource bindings complete; B-2 post-merge cleanup landed; B-3 merged; B-4 in PR
**Last updated:** 2026-05-17 (B-4 — output validation pipeline + input safeguards)

---

## Phase status

| Phase | Status | Notes |
|-------|--------|-------|
| A — research and design | ✅ done | `design.md` merged into `claude/ts-prompt-assist-features` via [#357](https://github.com/ErikFortune/fgv/pull/357). All 6 OQs resolved; §15 documents ts-res `import` packlet audit; §17 (added 2026-05-16) documents the validator-chain redesign + NQ-5 resolution + restart guardrails. |
| B — implementation | 🟡 restart in progress (B-0a/B-0b/B-1a/B-1b/B-2 done; B-3 in PR) | First attempt (PR #359) retired without merge after ~35 reviewer-flagged issues. Rescoped into sub-phase commissions (B-0a through B-5) per `brief-phase-b.md`. B-0a (NQ-5 audit) done by orchestrator 2026-05-16. **B-0b complete 2026-05-16** (ts-extras additive: `MustacheTemplate.create({escape})` + `extractJsonText` public-export confirmation). **B-1a complete 2026-05-16** via [#362](https://github.com/ErikFortune/fgv/pull/362) — foundation minus ts-res integration; B-1 agent surfaced the ts-res scope deferral honestly. Orchestrator formalized B-1a / B-1b split post-merge. **B-1b complete 2026-05-16** — ts-res integration via long-lived `ResourceManagerBuilder` per design §15.5 Option C + all 16 PR #362 Copilot deferrals closed + `resolve()` refactored to Result chaining. 91 tests, 100% coverage, all gates green. **B-2 complete 2026-05-17** via [#366](https://github.com/ErikFortune/fgv/pull/366) — recursive resource-binding resolver with RFC 8785 canonical-JSON cycle keys, depth cap, OQ-2 strict-replace, full inner-trace surfacing. 108 tests, 100% coverage. **B-3 commissionable** (FileTreePromptStore disk path / FsTree smoke test / YAML loader / filename-id consistency). |
| Refine — consumer-port pressure-test | ⏸ blocked on phase B publish | First-consumer port (an agent chat application) surfaces gaps; 1–2 follow-up PRs on the integration branch absorb refinements before integration→release promotion |

---

## Phase B restart history

| Date | Event |
|---|---|
| 2026-05-15 | Phase B first attempt commissioned against `brief-phase-b.md` via [#358](https://github.com/ErikFortune/fgv/pull/358) (single-agent run; Sonnet) |
| 2026-05-15/16 | Implementation produced [PR #359](https://github.com/ErikFortune/fgv/pull/359) targeting `release` (incorrect base; brief said integration branch) |
| 2026-05-16 | PR #359 reviewed; ~35 issues flagged across structural design-level violations, family-convention violations, correctness bugs, process violations. Erik closed PR #359 "retire and regroup" |
| 2026-05-16 | Orchestrator rescope: brief amended with 10 explicit guardrails + sub-phase decomposition; design.md §17 added with validator-chain redesign + NQ-5 resolution + kind-naming decision. B-0b commissionable next. |
| 2026-05-16 | **B-0b completed.** ts-extras additive: `MustacheEscapeStrategy` type + `escape?` option on `IMustacheTemplateOptions`; per-instance `Mustache.Writer` with locally-implemented `HTML_ESCAPE` so the strategy is immune to `Mustache.escape` global mutation (notably from `experimental/formatter.ts`); 8 new tests cover all three strategies (`'html'` back-compat, `'none'` verbatim, custom function) + concurrent-template safety + `validateAndRender` integration + triple-brace always-unescaped. `extractJsonText` confirmed already `@public` and re-exported from the ai-assist packlet — no source change needed; api.md regenerated. Rush change file added. Build / lint / fixlint / test all green; 100% coverage on `mustache` packlet; code-reviewer approved (no Priority 1/2 findings). Work branch: `claude/implement-b-0b-agent-R8kzX`. |
| 2026-05-16 | **B-1 foundation landed (partial).** New `@fgv/ts-prompt-assist` package scaffolded under `libraries/ts-prompt-assist/` with `sideEffects: false`, registered in `rush.json` under `versionPolicyName: base-utils` / tag `libraries`. Full type system (branded scalars + Converters; string-union types + `allFooValues` + Converters; discriminated-union slot-binding Converter routing on `kind`; descriptor / examples / trace / store-event types). `IPromptRegistry<TResponse extends { kind: string }>` per design §17.2 (three typed sub-registries, ConverterRegistry tracks producing kind via `register(id, kind, converter)` and exposes `getKind`; OutputValidationRegistry typed against `TResponse` with `appliesTo`). `descriptorConverter` + `promptFileConverter` (rejects free-text descriptors that declare `outputValidations` and rejects unknown output kinds); `bodyTokenScanner` rejects double-brace `{{name}}` (post-parse via `tokenType === 'name'`) AND ampersand-unescape `{{&name}}` (pre-parse regex; design-vs-mustache.js gotcha — mustache.js's tokenizer collapses `{{&name}}` and `{{{name}}}` to the same `'&'` tokenType, so the rejection cannot rely on parser metadata alone; this is the "name vs &" wrinkle B-3 should surface to the orchestrator if a stricter post-parse discriminator is desired). `bindingsFileConverter` / `axesConverter` (delegates to ts-res's `Qualifiers.Convert.qualifierDecl` — does NOT shadow). `PromptStoreFixture.build` over `InMemoryFileTree` (no standalone `InMemoryPromptStore`). Read-only `FileTreePromptStore` (`get` / `list` / `getBindings` / `getQualifierConfig`; default scope encoding rejects `/ \ \0 :`, leading `.`, non-POSIX-portable chars, and reserved Windows device names — basename portion checked so `CON.txt` is also rejected; consumer-supplied encoder/decoder pair supported and threaded through `PromptStoreFixture` via `scopeDecoding?`). `MustacheTemplateCache` LRU keyed by `(promptId, Crc32Normalizer.computeHash(body))`, default cap 256 per design §6.4. `walkScopeChain` + `mergeBindings` (cross-scope merge with `enforced` lock + `winningScope` trace + `enforced-override-ignored` safeguard finding); `PromptLibrary.create` / `describe` / `resolve` end-to-end via the in-memory fixture: chain walk → binding merge → intra-record candidate selection (specificity-ascending, terminal stops) → Mustache render via `escape: 'none'` → full `IPromptResolveTrace` with `winningScope` / `scopesConsulted` / `mergedBindings` / `safeguardFindings` / `candidateMatches`. 71 tests, 100% coverage on statements/branches/functions/lines. Code-reviewer agent ran on `7de1f88a`; 2 Priority 1 findings fixed in `86927dc9`. Copilot pull-request reviewer ran twice on PR #362; mechanical findings fixed in `8a7827a0` + `cc0df79c`; substantive findings annotated inline with `// Copilot review (PR #362, deferred to B-1b)` breadcrumbs in `6403c851` + `cc0df79c`. **Deferred to B-1b** (catalogued in detail in `brief-phase-b-1b.md`): (a) full ts-res integration via the long-lived `ResourceManagerBuilder` per §15.5 Option C — the current B-1 candidate selector is a stand-alone matcher that does NOT integrate with ts-res's qualifier types, `scoreAsDefault`, priority, or the intrinsic O(1) caches; (b) ~16 Copilot deferrals spanning `SlotName` Mustache-identifier validation, `brandedString` minimal hygiene, `MustacheTemplateCache` `::` key collision risk, `ConverterRegistry.get<T>` cast surface, `promptFileConverter` double-traversal, `_slotBindingHolder` mutable-singleton pattern, `joinBodies` trim regex narrow contract, `PromptStoreFixture` eager `mapResults`, `chainWalker` sequential `getBindings`, `describe` cache invalidation, scope-encoding `COM0`/`LPT0` widening, and several intent-clarification doc notes; (c) resource-binding resolve (B-2 scope) and output validation pipeline (B-4 scope) continue to fail loudly per Guardrail #4. Work branch: `claude/implement-phase-b1-foundation-1tZfR`. PR: [#362](https://github.com/ErikFortune/fgv/pull/362). |
| 2026-05-16 | **B-1b commissioning.** Brief authored at `.ai/tasks/active/ts-prompt-assist/brief-phase-b-1b.md`. Scope: (Part 1) replace the standalone candidate selector with full ts-res integration per design §15.5 Option C — long-lived `ResourceManagerBuilder` inside `PromptLibrary`, lazy materialization via `addLooseCandidate`, `ResourceResolver` instantiation with caller qualifier context, `'matchAsDefault'` trace surfacing, `IConditionMatchResult[]` per-candidate trace. (Part 2) sixteen Copilot deferrals from PR #362, each catalogued with file:line + recommended fix path. Expected surface changes: `IPromptLibraryCreateParams` gains `qualifiers` / `qualifierTypes?` / `cacheListener?` / `logger?` / `resourceBindingDepthLimit?` per design §4.1; `Convert.slotName` tightens to Mustache-identifier; `Convert.promptId` rejects `::`; `ConverterRegistry.get<T>` gains a kind-verified overload; `PromptStoreFixture` threads `qualifiers` through to `PromptLibrary.create`. Out of scope for B-1b: resource-binding resolve (B-2), FsTree smoke test + filename-id consistency (B-3), output validation pipeline + input safeguards (B-4), docs + change file (B-5). Commissionable once PR #362 merges to the integration branch. |
| 2026-05-16 | **B-1b — design §10.2 amended; `isPartial` semantic re-aligned to ts-res.** PR #364 Copilot review surfaced a perceived "regulars-vs-defaults" ordering issue. Investigation with Erik clarified the underlying confusion: the design's §10.2 partial-walk had imported `isPartial` from ts-res by name but inverted the semantic (partial = base layer instead of override layer). Decision (Erik): align with ts-res's natural `resolveComposedResourceValue` semantic — `isPartial: true` marks a more-specific override that layers ONTO the full base; `isPartial: false` (or omitted) marks the full base body. The walk consumes ts-res's natural priority-descending order (`resolveAllResourceCandidates` shape: regulars then defaults), collects until first non-partial (full → stop), then reverses for the join so the base appears first and the most-specific override appears last. design.md §5.3 YAML example and §10.2 algorithm updated to match. Rationale: same-name-opposite-semantic between ts-res and ts-prompt-assist would compound author confusion and lead to a long tail of corner cases where the two libraries' composition models drift. Hand-rolled "preference filter" from earlier in B-1b removed. |
| 2026-05-16 | **B-1b PR #364 opened and converged after 5 Copilot review rounds + 1 code-reviewer pass.** Each round closed substantive findings, not just polish — the architecture survived but the implementation hardened materially. Highlights worth carrying forward: (R1) Hand-rolled `isPartial` walk semantic was inverted relative to ts-res's `resolveComposedResourceValue` model; Erik confirmed the design amendment (partial = more-specific override layering onto a full base; design §10.2 + §5.3 rewritten). (R2) CRC32 hashes replaced with RFC 8785 canonical-JSON for descriptor cross-scope equality + materialization cache key — CRC32 isn't collision-resistant, false-positive equality would alias structurally-distinct records. Synthesized ts-res resource ids switched from CRC32-derived to sequential `prompt_<n>` (no collisions possible). (R3) Synth-id reservation map keyed by canonical cache key so retries of a malformed record reuse the same id instead of allocating fresh ones (bounds orphan resources). (R4) `canonicalize` rejects `undefined`; sanitize via `sanitizeJsonObject` (JSON round-trip) before canonicalize so in-memory descriptors / candidates with explicit-`undefined` optionals work cleanly. Origin-index map preserves the FIRST authored index when ts-res dedupes byte-identical candidates. (R5) Pre-validate qualifier names against `_qualifierCollector.hasNameOrToken` in phase 1 of materialization — eliminates the dominant `addLooseCandidate` failure mode before any builder mutation, so a malformed record leaves ZERO orphan conditions/decisions/candidates. `IPromptCandidateRecord.isPartial` public TSDoc realigned with the §10.2 amendment. Final state: 91 tests, 100% coverage, no api-extractor warnings, build/lint clean. Branch ready for merge into `claude/ts-prompt-assist-features`. |
| 2026-05-17 | **B-2 complete.** Recursive resource-binding resolver wired into `PromptLibrary.resolve`. `bindingMerger` refactored to defer resource-binding serialization: literal bindings serialize synchronously as before; resource bindings survive merge with placeholder `value: ''` and surface in a new `pendingResourceBindings` array on `IBindingMergeResult`. `IBindingMergeResult.merged` stays a `ReadonlyMap` (the public type surface remains immutable); the per-slot rewrite strings flow out of the resolver as a separate `ReadonlyMap<SlotName, string>`, and `PromptLibrary._renderResolved` folds them into a fresh `Map` copy before render so the trace's `mergedBindings` is a structurally-final immutable view. New `resourceBindingResolver.ts` module: `buildCycleKey` computes the RFC 8785 canonical-JSON string for `{ chain, id }` via `Crc32Normalizer.canonicalize` (NOT CRC32 — per B-2 brief override of design §7 step 2; consistent with B-1b's descriptor and materialization cache keys; collision-free); `resolvePendingResourceBindings` drives the per-binding inner resolve via an `InnerResolveFn` callback, applies OQ-2 strict-replace substitution semantics (binding-supplied `substitutions` → mode `'replace'`; omitted → mode `'inherit'`, inner inherits the parent's `req.substitutions`), returns a per-slot `rewrites: ReadonlyMap<SlotName, string>` and emits `IResourceBindingTraceEntry[]` with the full inner trace. `formatCycleError` builds the human-readable cycle path. `PromptLibrary.resolve` splits into a thin wrapper over `_resolveInternal(req, depth, stack)`; each entry checks `depth > resourceBindingDepthLimit` (default 5) and rejects with "resource binding depth limit (N) exceeded", computes the canonical key, rejects if already on the stack ("cycle detected in resource binding chain: 'a' -> 'b' -> ... -> 'a'"), push/pop via try-finally so sibling slots don't false-trigger. After `mergeBindings`, `_resolveResourceBindings` runs the pending inner resolves through `_resolveInternal` at `depth + 1`; inner resolves reuse the SAME long-lived `ResourceManagerBuilder` so materialization cache hits transparently across recursion (verified: outer with two slots binding to the same inner materializes inner once). Design §7 step 6 enforced: a resource binding referencing a `'json'`-output descriptor fails at resolve time with a clear error. `ResourceId` → `PromptId` validation via `Convert.promptId.convert` so resource bindings citing invalid prompt ids (e.g. containing `'::'`) surface loudly. 14 new tests in `resourceBindings.test.ts` cover happy path, strict-replace, inherit, cycle (A→B→A), default-depth cap (7-deep chain rejected at 5), configurable depth limit (1 rejects 2-deep, 2 succeeds), materialization cache hit, `enforced-override-ignored` on a resource-bound slot, `scopeOverride`, qualifier override, json-output reject, invalid-resource-id with `'::'`, inner-resolve failure context propagation, 3-deep A→B→C verifying `innerTrace.resourceBindingResolutions` recursively populates. The `bindingMerger` failure-path coverage was tightened with three additional tests (caller-sub / default / enforced+caller paths through `installBinding`). Total 108 tests across `foundation.test.ts` (94) + `resourceBindings.test.ts` (14); 100% coverage on statements / branches / functions / lines. `rushx build`, `rushx lint`, `rushx fixlint`, `rushx test` all green. api-extractor regenerated. Out-of-scope items deferred per brief: FsTree smoke test / filename-id consistency (B-3); output validation pipeline / safeguards beyond `enforced-override-ignored` (B-4); LIBRARY_CAPABILITIES / README / rush change file (B-5). **Carry-forward gate added to B-4 brief row:** `IBindingMergeResult.mergedBindings: ReadonlyMap<SlotName, SlotBinding>` is built but unread (B-1 carryover, preserved through B-2). B-4 MUST either consume it (e.g. to feed `IOutputValidationContext.substitutions` with raw `SlotBinding` shape) and document the consumer, OR remove the field and stop populating it. Work branch: `claude/implement-resource-binding-b2-MrDaL`. |
| 2026-05-17 | **B-3 in PR.** Scope: filename-id consistency check + FsTree on-disk smoke test. (1) `verifyFilenameId` free function in `fileTreePromptStore.ts` rejects any prompt file whose `descriptor.id` doesn't match the filename stem (`file.baseName`); error message cites both values and the file's `absolutePath` so authors can locate the divergence immediately. Wired into both `_readPromptFile` (driven by `get(scope, id)`) and `_listScope` (driven by `list()`), so the rule fires regardless of access path. (2) New `src/test/unit/store.test.ts` exercises `FileTree.forFilesystem()` against on-disk YAML fixtures under `data/test/ts-prompt-assist/basic/` (mirrors ts-res's fixture layout — `data/test/<package>/...`). Covers all four public read methods (`get`, `list`, `getBindings`, `getQualifierConfig`) end-to-end through the disk-backed FileTree adapter; verifies the `_qualifiers.yaml` root-level path established in B-1a carries over verbatim from in-memory to disk. (3) Filename-id mismatch tests use `FileTree.inMemory` so the deliberate divergence between filename stem and descriptor.id is authored without touching disk. (4) Double-brace rejection (both `tokenType === 'name'` for `{{name}}` and the ampersand-unescape regex catch for `{{&name}}`) verified at foundation.test.ts:182-184 (already covered in B-1a — no new code needed; the design-vs-mustache.js gotcha that B-1a flagged is genuinely resolved by the two-mechanism approach the body scanner ships). (5) Out-of-scope items honored per Guardrail #4 (no silent stubs): `put` / `putBindings` / `delete` / `watch` remain undefined; output validation pipeline + safeguards beyond `enforced-override-ignored` deferred to B-4; LIBRARY_CAPABILITIES + README + rush change file deferred to B-5. 115 tests (108 + 7 new), 100% coverage on every file (statements / branches / functions / lines). `rushx build`, `rushx lint`, `rushx fixlint`, `rushx test` all green. api-extractor regenerated (no public surface diff — `verifyFilenameId` is a packlet-internal free function). code-reviewer agent ran on the diff — approved with one P3 cosmetic note (inline `import()` in test for `SlotName` typing); addressed before final commit. Work branch: `claude/implement-b3-subphase-33P5V`. |
| 2026-05-17 | **B-2 post-merge cleanup (orchestrator).** Three small follow-ups surfaced during B-2 triage, fixed in a single PR rather than carried into B-3/B-4 (the bookkeeping outweighs the cost). (1) Removed dead public surface `IBindingMergeResult.mergedBindings: ReadonlyMap<SlotName, SlotBinding>` — built in `bindingMerger.ts` but consumed by zero call sites and zero tests since B-1; the B-2 agent had added a carry-forward gate to B-4 to decide its fate, but with no consumer in sight the bookkeeping isn't worth carrying forward. Field + plumbing removed; `installBinding` signature shrank; api.md regenerated. The B-4 carry-forward gate is therefore retired. (2) `Hash.Crc32Normalizer` → base `Normalizer` (from `@fgv/ts-utils`) at the four canonical-JSON-only sites (`PromptLibrary._normalizer`, `buildCycleKey` parameter, the descriptor-cache TSDoc breadcrumb). CRC32 hashing never actually participates at those sites — only `.canonicalize()` is called — so the class choice was misleading; the base `Normalizer` exposes `canonicalize` directly. `MustacheTemplateCache` still uses `Hash.Crc32Normalizer.computeHash` (legitimate hash use — LRU key on body strings). (3) design.md amendments: §7 step 2 rewritten to specify RFC 8785 canonical-JSON cycle keys (matching what B-2 actually shipped, overriding the original CRC32 spec); §10 #11 reworded so the "no `JSON.stringify`-compare" rule names canonical-JSON for structural equality and clarifies CRC32 is for hash-only use cases; §15.5 step 2 rewritten so the materialization cache key spec matches B-1b's canonical-JSON implementation + sequential synth-id reservation. Gates: `rushx build` (deps + ts-prompt-assist) green; `rushx test` 108/108 with 100% coverage on every file; `rushx lint` green. No test churn — the removed field had no test coverage to begin with (which is what made it dead surface). Work branch: `chore/ts-prompt-assist-b2-cleanups`. |
| 2026-05-17 | **B-4 PR #369 review responses landed.** Copilot review + Erik review surfaced six threads; five addressed in `e4afc6a4`'s follow-up commit, one (the entangled validator-chain caller-T type-safety hole) deferred per Erik's direction to a **P1 urgent tech-debt entry** in `docs/TECH_DEBT.md` (covering the `ConverterRegistry.get` cast Erik flagged + the no-kind `get<T>` use in `runOutputValidationPipeline` Erik +1'd Copilot on + the free-text `rawOutput as unknown as T` return-type lie Copilot flagged — all three are the same family: caller-controlled `T` is not runtime-verified, which is the precise PR #359 failure-mode family that triggered the design §17.2 redesign). Fixes shipped: (a) **Double `prompt '${id}'` prefix in safeguard error messages** — `applySafeguards` already formats its rejecting findings with the prompt id, so the outer `.withErrorFormat((msg) => \`prompt '${req.id}': ${msg}\`)` wrapper in `_renderResolved` was producing `prompt 'p': prompt 'p': ...` noise. Wrapper removed; inline comment cites PR #369. (b) **Length-cap validation** — `slot.maxLength` / `descriptor.safeguards.defaultMaxLength` / `policy.defaultMaxLength` are plain `number` so `NaN` silently disabled the cap (`value.length > NaN === false`) and negatives rejected every non-empty value. `applySafeguards` now calls `isFiniteNonNegativeInteger(lengthCap)` before applying and rejects loudly at apply-time with `prompt '${id}': slot '${slot}': maxLength must be a finite non-negative integer (got ${value})`. (c) **`enforced-override-ignored` co-existence test** that didn't actually trigger the merge-stage finding — the fixture supplied no scope bindings, so merge had nothing to override-ignore. Rewritten to seed an enforced scope binding via the fixture and a caller substitution for the same slot; assertion now verifies BOTH `enforced-override-ignored` AND `suspicious-pattern` findings land in `trace.safeguardFindings`, AND that the merge-stage finding precedes the safeguard-engine finding (deterministic ordering). One new test added for the length-cap validation (NaN / -1 / 3.5 all reject with the expected error). 149 tests, 100% coverage maintained across statements / branches / functions / lines. Work continues on the same branch `claude/implement-b4-subphase-PF7MV`. |
| 2026-05-17 | **B-4 in PR.** Output validation pipeline (design §8 + §17.2) and input safeguards (design §9) landed. (1) New `packlets/output/outputPipeline.ts` exports `assertOutputValidationsCompatible(descriptor, registry)` (loader-side belt per §17.2.4 — verifies every `outputValidations[]` validator's `appliesTo` covers the producing-kind declared by the descriptor's `output.converterId`'s registered Converter; rejects unregistered converters/validators with a clear error citing both) and `runOutputValidationPipeline({ descriptor, contract, registry, rawOutput, substitutions })` (uses `AiAssist.fencedStringifiedJson({ inner: converter })` from `@fgv/ts-extras` to fence-strip + `JSON.parse` + dispatch through the registered Converter; chains validators with `MessageAggregator` for failure aggregation; runtime-suspenders check re-verifies `value.kind ∈ validator.appliesTo` per §17.2.4). End-to-end type flow with NO cast: `registry.converters.get<T>(converterId)` returns `Converter<T>` (no-kind overload, trusts caller's `T`), `fencedStringifiedJson<T>({ inner: converter })` returns `Converter<T>`, validator chain narrows via a typed `isReadonlyArrayOfKind` guard rather than `Array.isArray(...) as ...`. `PromptLibrary.resolveAndValidateOutput<T extends TResponse>`: free-text branch returns `succeed(rawOutput as unknown as T)` per design §8 (the only documented unchecked cast — TResponse exists only to type the JSON pipeline; free-text has no Converter); JSON branch dispatches into `runOutputValidationPipeline`. (2) New `packlets/safeguards/safeguardEngine.ts` exports `applySafeguards(descriptor, merged, policy)` — per-slot length cap with precedence `slot.maxLength` → `descriptor.safeguards.defaultMaxLength` → `policy.defaultMaxLength` → none; regex screen with **`pattern.lastIndex = 0` reset between slots** (PR #359 retrospective bug); source-aware skipping — `slot.source` declared but not in `policy.screenedSources` OR `descriptor.safeguards.skipInjectionScreening: true` emits a `'screening-skipped'` info finding; slots without a declared source are silently not screened (nothing to skip). Length cap reject comes BEFORE regex screen (cheap reject first per design §9 #5). (3) New `packlets/types/safety.ts` ships `IPromptSafetyPolicy` + `SuspiciousDisposition`, plumbed through new `IPromptLibraryCreateParams.safetyPolicy?` create-param (optional — when omitted, no global cap / no screening / no preface). (4) Anti-jailbreak preface seam: `_applyAntiJailbreakPreface` invokes `policy.antiJailbreakPreface(descriptor)` after Mustache render; empty-string return leaves body unchanged; consumer-side failure surfaces with prompt id. (5) `_resolveOnce` adds a `_validateAgainstRegistry(descriptor)` step after `walkScopeChain` (cached per `PromptId` via `_validatedRegistryIds: Set` so the hot path pays the check at most once per id); `_populateDescriptorCache` also runs the validation, so `describe()` rejects loader-incompatible descriptors. (6) `_renderResolved` folds the safeguard findings into `trace.safeguardFindings` alongside the merge-stage `'enforced-override-ignored'` findings — all four `SafeguardFindingKind` values now flow through the trace. **No unchecked casts anywhere in the validator chain** (the design §17.2 PR #359 failure mode). **No `Result<void>`, no `console.*`, no `unknown` without rationale, no manual type checks.** 33 new tests in `outputAndSafeguards.test.ts` cover: fence-strip happy path; trailing prose tolerance; JSON.parse failure surfaces prompt id + raw-output snippet; 500-char raw output truncates to 200 with `…`; Converter mismatch on output kind; validator chain rejection aggregates; missing converter id at runtime; loader-side `appliesTo`/converter-kind mismatch (belt); loader-side unregistered validator id (belt); loader-side fires from `describe()`; loader-side cached on subsequent calls; json-without-registry rejects; runtime-suspenders catches Converter that lies about producing kind (test uses an intentional `as unknown as T` per CODING_STANDARDS test-files exception to deceive the runtime); per-slot maxLength + descriptor.defaultMaxLength + policy.defaultMaxLength precedence ladder; regex screen warn/reject dispositions; `lastIndex` reset between slots (regression test for PR #359 bug — uses stateful `/g` regex on two slots, both must match); pattern-present-no-match path; source-not-screened → `'screening-skipped'` info; `skipInjectionScreening` → `'screening-skipped'` info; no source declared → silent (no finding); anti-jailbreak preface prepend; empty-string preface no-op; preface failure surfaces; length-cap before regex screen ordering; empty-source slot silently skipped; co-existence with `enforced-override-ignored`. Two B-1 placeholder tests in `foundation.test.ts` updated to assert new behavior (free-text returns raw verbatim; json-without-registry rejects clearly). 148 tests total at original push (149 + further as the review-response commits land — see the row above for the current count), **100% coverage on statements / branches / functions / lines** for every file (verified). `rushx build`, `rushx lint` (clean — no warnings), `rushx fixlint`, `rushx test` all green. api-extractor regenerated (no warnings — `{@link}` references rewritten to prose where the link target was not a public export). code-reviewer agent invoked on the diff. Out-of-scope items honored per Guardrail #4: `LIBRARY_CAPABILITIES.md` + README + rush change file deferred to B-5; editor-UX surfacing of safeguard findings deferred to consumer concern; LLM-call wiring is consumer concern (B-4 ships the seam, not the call). Work branch: `claude/implement-b4-subphase-PF7MV`. |
| 2026-05-16 | **B-1b complete.** Part 1 (ts-res integration per design §15.5 Option C / §17.1): `PromptLibrary` now holds a long-lived `ResourceManagerBuilder` parameterized by the new required `qualifiers` create-param (accepts `IReadOnlyQualifierCollector | IQualifierDecl[]`; the decl path also requires `qualifierTypes`). Per-resolve flow: compute the RFC 8785 canonical-JSON string for `{ scope, id, candidates }` → look it up in the materialized-resource map → cache miss validates each candidate via `ResourceJson.Convert.conditionSetDecl` and calls `addLooseCandidate` per candidate under a sequential synthesized id `prompt_<n>` reserved per canonical key (reused on retry so malformed records don't accumulate orphan resources); cache hit reuses the materialization. Canonical-JSON is used in place of CRC32 because CRC32 isn't collision-resistant — false-positive equality would let the id-only descriptor cache or the synthesized-id reservation alias structurally-distinct records. A `ResourceResolver` is instantiated per-resolve over the long-lived builder + a `ValidatingSimpleContextQualifierProvider` built from the caller context; `resolveDecision(resource.decision.baseDecision)` returns priority-descending instance indices which the library reverses to design §10.2's specificity-ascending walk (collecting bodies until `isPartial !== true`). Trace `candidateMatches[i].matchType` now correctly surfaces `'match'` vs `'matchAsDefault'` per ts-res's actual disposition, and `candidateMatches[i].conditions` is populated from `resolver.resolveConditionSet(conditionSet).matches` (both were placeholders in B-1a). The standalone in-record matcher is REMOVED — no dual-path code. New create-params per design §4.1: `qualifiers` (required), `qualifierTypes?`, `cacheListener?`, `logger?` (defaults to `Logging.NoOpLogger`), `resourceBindingDepthLimit?` (default 5; wiring-only until B-2). Part 2: sixteen Copilot deferrals from PR #362 closed — 2.1 `SlotName` tightened to Mustache "name" production; 2.2 `brandedString` enforces max-length + whitespace + `PromptId` rejects `::`; 2.3 `MustacheTemplateCache` key collision-free via 2.2 (comment-only path); 2.4 `ConverterRegistry.get` gained kind-verified overload; 2.5 `promptFileConverter` no longer double-traverses; 2.6 `_slotBindingHolder` replaced by `_bindingDispatch` with cleaner explanatory comment (closure-thunk alternative trips ESLint no-use-before-define under our config; the holder pattern is the residual-cleanest path); 2.7 `joinBodies` widened to `/\s+$/`; 2.8 closed by Part 1's ts-res priority ordering; 2.9 `IQualifierContext` flat-string converted to design rationale; 2.10 `PromptStoreFixture` chained via three encode helpers; 2.11 `chainWalker` parallelizes `getBindings` via `Promise.all`; 2.12 `describe` validates structural equality across scopes + adds `invalidateDescriptor`; 2.13 reserved Windows device set widened to `COM0..9` / `LPT0..9`; 2.14 closed as documentation (async-with-sync-body is forward-compat for FsTree); 2.15 closed as design rationale per §10.4; 2.16 closed (resolve-first is the correct shape per B-4). Part 3: `resolve()` refactored to Result chaining (Guardrail #10) — single `walkScopeChain().onSuccess(walked → mergeBindings().onSuccess(merge → materializeIfNeeded().onSuccess(materialized → resolveCandidates().onSuccess(matches → renderResolved))))` pipeline replaces the imperative `isFailure()` cascade. `PromptStoreFixture` continues to thread the `qualifiers` seed into the in-memory FileTree; tests pass a shared `TEST_QUALIFIER_COLLECTOR` built from three permissive `LiteralQualifierType`s (`lang`, `tone`, `region`). 91 tests, 100% coverage on statements / branches / functions / lines; `rushx build`, `rushx lint`, `rushx fixlint`, `rushx test` all green. api-extractor regenerated. Work branch: `claude/b1b-foundation-hardening-7F4XM`. |

---

## Cluster context

**Cluster name:** `ts-prompt-assist-features`
**Integration branch:** `claude/ts-prompt-assist-features` (off `release`; created by orchestrator before phase A commission)
**Workflow shape:** design-triage-implement-refine (per Erik 2026-05-13)

---

## Decisions log (orchestrator, substrate prep)

| Decision | Rationale |
|---|---|
| Standalone package above `ts-res` | Folding into `ts-extras` would create a cycle (`ts-res` already depends on `ts-extras`). Confirmed in design-brief.md §"Why a separate package" |
| Workflow shape: design-triage-implement-refine | Conceptual model is binding but data shapes are proposed; 3+ open questions warrant a phase A signoff before implementation. Consumer-port pressure-test ("refine") absorbed via integration-branch follow-up PRs |
| Integration branch (vs direct-off-release stream) | Cluster expected to be multi-stream (library v0.1 → consumer-port refinements → possibly samples / editor UX). Long-lived branch allows v0.1 alpha + pressure-test absorption + v0.2 to ship as one cohesive merge to `release` |
| First-consumer port = an agent chat application | Pressure-test plan: 1–2 follow-up PRs on the integration branch absorb gaps surfaced by the port |
| ts-extras `mustache` packlet is in cluster scope | Erik's direction (2026-05-13): "instead of dancing around ts-extras mustache limitations, fix ts-extras and use the fixed API." Cluster will additively extend `MustacheTemplate` to support verbatim-passthrough rendering; `ts-prompt-assist` consumes the extended API. Phase A picks the specific API shape (OQ-6 candidates a/b/c) |
| Active surface registration | Added `ts-prompt-assist` to `.ai/instructions/ACTIVE_DEVELOPMENT.md` table (free hand on breaking changes during v0.x) |
| Alpha target | `5.1.0-29` or later; may accumulate to `6.0` based on API-stability evidence after pressure-test |
| Sequencing | Independent of `ai-assist-thinking-events` (queued). Streams can run in parallel; no conflicting surface |
| Future streams (queued) | `ts-prompt-assist-samples` and `ts-prompt-assist-editor-ui` — both in `docs/FUTURE.md` |

---

## Open questions

### Orchestrator-flagged for phase A (must resolve)

| ID | Question | Source |
|---|---|---|
| OQ-1 | Scope encoding flexibility — flat default vs nested directories | design-brief.md §"Open questions" |
| OQ-2 | Resource-binding substitution merge semantics — strict (replace) vs relaxed (layered) | design-brief.md §"Resource-binding semantics" |
| OQ-3 | `watch()` semantics — include in interface? optional? full event-shape definition? | design-brief.md §"Open questions" |
| OQ-4 | Three separate registries vs one unified `IPromptRegistry` with namespaced sub-registries | orchestrator review |
| OQ-5 | Output-contract growth path — is the `IJsonOutputContract<TKIND>` generic pulling its weight at v0.1? | orchestrator review |
| OQ-6 | Mustache canonical form — extend ts-extras `MustacheTemplate` for verbatim-passthrough; pick API shape (option on create / strict-passthrough mode / sibling primitive) | orchestrator review (Erik direction: extend ts-extras, don't work around it) |

### Deferred (not phase A's concern)

| ID | Question | Where it lives |
|---|---|---|
| F-1 | Samples app shape — standalone vs folded into `ai-assist-image-generator` | `docs/FUTURE.md` (post-v0.1 stream) |
| F-2 | Generic editor UX feasibility — `@fgv/ts-prompt-assist-ui` or consumer-specific | `docs/FUTURE.md` (post-v0.1 stream) |

---

## Phase A decision log (agent, 2026-05-15)

| OQ | Decision | Summary |
|---|---|---|
| OQ-1 | flat default + function override | Default rejects `/ \ \0 :` + reserved Win device names + non-portable POSIX chars; consumers supply `scopeEncoding` / `scopeDecoding` for nested-directory layouts. Rationale: simplest path that doesn't paint v0.2 into a corner; tightening defaults later is breaking. |
| OQ-2 | strict (replace) | Binding-supplied `substitutions` entirely replace parent's for inner resolve; trace records `'replace'` vs `'inherit'` mode. Rationale: clean mental model for shared fragments; relaxing later is additive. |
| OQ-3 | interface includes `watch?`; **no v0.1 adapter implements**; event shape pinned (revised 2026-05-15 per Erik review) | `IPromptStoreEvent.kind: 'descriptor-changed' \| 'descriptor-removed' \| 'bindings-changed' \| 'qualifier-axes-changed'`. No standalone InMemory store (per Erik), so no v0.1 driver for watch impl; pinning the event shape costs nothing and prevents future interface churn. First concrete hot-reload consumer drives implementation in a follow-up. |
| OQ-4 | unified `IPromptRegistry` with **three** typed sub-registries (revised 2026-05-15 per Erik review); qualifier config delegated to ts-res | `IPromptRegistry` exposes `converters` / `slotKinds` / `outputValidations`. **`qualifierEnums` sub-registry dropped** — ts-res's `LiteralQualifierType` already owns closed-value qualifier sets; duplicating in ts-prompt-assist forced two-place registration (drift bug). `PromptLibrary.create` takes a `qualifiers: IReadOnlyQualifierCollector \| IQualifierDecl[]` param directly. Per-scope `_qualifiers.yaml` replaced by ONE root-level `<root>/_qualifiers.yaml` using ts-res's `IQualifierDecl` schema (validated by ts-res's own Converter); the file is optional. `IPromptStore.getQualifierAxes` becomes `getQualifierConfig(): IQualifierDecl[] \| undefined`. |
| OQ-5 | drop the generic on `IJsonOutputContract`; minimal free-text; `outputValidations` on descriptor | Future JSON variants extend the discriminated union (`'json-array'`, `'json-stream'`) rather than parameterizing. `outputValidations` stays on `IPromptDescriptor`, not on the JSON contract, to keep growth open for future free-text validators. |
| OQ-6 | shape (a) — `escape?` option on `MustacheTemplate.create` | Additive `escape: 'html' \| 'none' \| (s) => string` on the canonical primitive in ts-extras. Default `'html'` preserves back-compat. Implementation MUST use `Mustache.Writer` instance, not `Mustache.escape` global. Double-brace rejection stays in ts-prompt-assist (consumer-discipline concern, not Mustache concern). |

## New questions surfaced (non-blocking)

| ID | Question | Where it lives |
|---|---|---|
| NQ-1 | `extractJsonText` export status from `@fgv/ts-extras/ai-assist` — currently public or internal? | design.md §8 step 1; phase B verifies / promotes |
| ~~NQ-1~~ | ~~extractJsonText~~ — **RESOLVED** (Erik 2026-05-15): export it publicly from `@fgv/ts-extras/ai-assist`. Additive; in cluster scope. | design.md §15.6 |
| ~~NQ-2~~ | ~~YAML loader~~ — **RESOLVED** by §15 audit: use `@fgv/ts-extras`'s `yaml.yamlConverter<T>(inner)`. Erik clarifies: ts-extras wraps js-yaml; not in ts-json-base because of the dep. | design.md §15.3, §15.6 |
| ~~NQ-3~~ | ~~`FileTreeItem` path~~ — **RESOLVED** (Erik): `@fgv/ts-json-base/file-tree`, symbol `FileTree.FileTreeItem`. | design.md §15.6 |
| NQ-4 | `PromptRegistry.empty()` infallible factory — keep alongside `.create()` or collapse? | design.md §4.3; default: keep both for v0.1 |
| NQ-5 (revised) | **Option C locked** — lazy materialization into a shared long-lived ts-res `ResourceManager`. Phase B verifies incremental add-after-build (or extends ts-res additively; cluster scope). Fallbacks documented in §15.5: (ii) periodic rebuild, (iii) Option A with `TECH_DEBT.md` entry. | design.md §15.5 |

## Phase A re-audit: ts-res `import` packlet (2026-05-15, addendum)

Triggered by orchestrator prompt asking whether the loader reinvents ts-res's import packlet.

**Verdict:** partial overlap; **the loader does NOT reinvent ts-res import**. They target different access patterns (lookup vs build), different data shapes (descriptors + bindings + axes + candidates vs ts-res `ResourceManagerBuilder`-targeted decls), and different filename conventions (per-candidate `conditions:` blocks in YAML vs ts-res's filename-token-encoded conditions). The reuse target is the **lower-level primitives** ts-res import depends on, not the ImportManager pipeline itself.

**Recorded divergence (design.md §15):**

- `FileTreePromptStore` uses `FileTree` directly (not `PathImporter`).
- YAML parsing via `@fgv/ts-extras`'s `yaml.yamlConverter<T>` (not via `FsItemImporter`'s `fileContentConverter` seam).
- One YAML file per `(scope, id)` with descriptor + all candidates co-located; conditions inline per candidate (not filename-encoded).
- `IPromptStore.get(scope, id)` lookup-oriented (not a global `ResourceManagerBuilder`).
- No use of `ImportManager` / `CollectionImporter` (their target shape is wrong).

**Gap surfaced and pinned:** the candidate→ts-res-resolve handoff (NQ-5) was underspecified in earlier drafts. Now locked: **Option A** (per-resolve ts-res construction, cached by `(scope, id, Crc32Normalizer.computeHash(candidates))`). The `ResourceJson` candidate-decl Converters in ts-res are the reuse point for validating candidates into ts-res's shape. Phase B confirms exact public exports.

**Resolved NQ-2:** YAML loader is the existing `@fgv/ts-extras/yaml` packlet, not a new `js-yaml` direct dep. Per `/published-primitives-reflex`.

---

## Erik review checkpoint 2026-05-15

Erik reviewed the design and surfaced four points + three NQ resolutions:

| Point | Disposition |
|---|---|
| Trace integration with ts-res's step-by-step resolution logging | ts-res publishes `IResourceResolverCacheListener` (cache hit/miss/error/clear events on condition / conditionSet / decision caches) + per-condition match details on `ConditionSetResolutionResult`. ts-prompt-assist (a) accepts a `cacheListener?: IResourceResolverCacheListener` on `PromptLibrary.create`, (b) defaults to forwarding to ILogger at debug level, (c) surfaces ts-res's `IConditionMatchResult[]` per candidate in `IPromptResolveTrace.candidateMatches[]`. Editor surfaces can mirror ts-res-browser's view without ts-res additions. |
| `PromptLibrary` accepts an `ILogger` and uses it for observability | Pinned in §4.1 with the event taxonomy (debug: materialization, cache events, resource-binding entry/exit, safeguard findings, Mustache parse/render, store invocations; warn: disallowed-qualifier stripping, regex-screen matches under `'warn'`, ignored enforced overrides; error: resolve failures paired with the Result return). Default `Logging.noOpLogger`. |
| Separate `InMemoryPromptStore` vs `FileTreePromptStore + InMemoryFileTree` | Dropped standalone `InMemoryPromptStore`. Tests/dev use `FileTreePromptStore` over `InMemoryFileTree` via a `PromptStoreFixture.build(seed)` helper that serializes the seed into the in-memory tree. Cascades: OQ-3 revised (no v0.1 adapter implements watch); event shape still pinned. |
| §15.5 caching | **Option C locked.** One long-lived ResourceManager shared across all resources; lazy materialization on demand; ts-res's intrinsic caches do the heavy lifting (O(1) on warm qualifier shapes). Erik's explicit guidance: cache resources (cheap, bounded cardinality), NOT outputs (combinatorial in open qualifier space). NQ-5 revised: phase B verifies incremental add-after-build in ts-res or extends additively. Fallback strategies documented in §15.5. |

NQ-1 (extractJsonText): export publicly. NQ-3 (IFileTreeItem path): confirmed.

## Cluster-scope summary (for orchestrator)

The `ts-prompt-assist-features` cluster touches **two libraries**:

1. **`@fgv/ts-extras`** (existing, established surface) — strictly **additive** change to the `mustache` packlet: `escape?: MustacheEscapeStrategy` option on `MustacheTemplate.create` (default `'html'`, preserves back-compat). Implementation MUST use a per-instance `Mustache.Writer` rather than mutating `Mustache.escape` globally. No removed / renamed / behavior-changed exports.
2. **`@fgv/ts-prompt-assist`** (new library) — full v0.1 implementation per this design.

Phase B implementation order: ts-extras Mustache extension lands first (B.0), then ts-prompt-assist consumes it from the start (B.1 onward).

## Substrate prep checklist (orchestrator)

- [x] Add stream entry to `docs/WORKSTREAMS.md` (Active section, before `ai-assist-thinking-events`)
- [x] Add `ts-prompt-assist` to `.ai/instructions/ACTIVE_DEVELOPMENT.md` (Currently active surfaces table)
- [x] Add `ts-prompt-assist-samples` and `ts-prompt-assist-editor-ui` parking-lot entries to `docs/FUTURE.md`
- [x] Author `design-brief.md` (consumer-supplied, verbatim)
- [x] Author `brief.md` (orchestrator's phase A binding contract)
- [x] Author `state.md` (this file)
- [ ] Open substrate-prep PR → `release`
- [ ] After merge: create `claude/ts-prompt-assist-features` integration branch off post-merge `release` HEAD
- [ ] Commission phase A agent against the integration branch (kickoff prompt drafted by orchestrator)

---

## Phase B rescope decision log (orchestrator, 2026-05-16)

| Decision | Rationale |
|---|---|
| **NQ-5 resolved: Option C achievable with zero ts-res changes** | Orchestrator audit confirmed ts-res's `ResourceManagerBuilder` natively supports `addLooseCandidate` / `addResource` / `addCondition` / `addConditionSet` post-construction and implements `IResourceManager` itself. Phase B holds a long-lived builder inside `PromptLibrary`; calls `addLooseCandidate` on cache miss; uses the builder directly as the `IResourceManager` for a `ResourceResolver`. Fallbacks (ii) and (iii) from design §15.5 are documented but not the target. See design §17.1. |
| **Validator-chain redesigned: discriminated-union-typed registries** | First attempt (PR #359) produced `runResult.value as T` cast in the chain runner — the original `Result<unknown>` shape forced it. Redesign per design §17.2: registries parameterized by `TResponse extends { kind: string }`; consumers extend the union with their own response shapes; each validator declares `appliesTo: TResponse['kind']`; chain runtime narrows via `value.kind` lookup; no cast anywhere. Belt (loader-side reject) + suspenders (runtime fail on kind mismatch). |
| **`kind` discriminator naming kept universally** | Multiple `kind` fields now exist across the design (`output.kind`, `SlotBinding.kind`, `IPromptStoreEvent.kind`, `ISafeguardFinding.kind`, NEW consumer response `kind`). Decision: keep `kind` everywhere per TypeScript community convention + `@fgv/*` family convention; rename specific sites only if a real conflict surfaces during implementation. JSDoc clarifies discriminator role at definition. See design §17.2.7. |
| **Sub-phase decomposition replaces single-agent run** | PR #359 demonstrated mid-run context drift is a real failure mode in a single long agent run. Restart decomposes phase B into 5–6 separate agent commissions (B-0a orchestrator audit; B-0b ts-extras additive; B-1 foundation; B-2 resource bindings; B-3 FileTreePromptStore + YAML; B-4 output validation + safeguards; B-5 docs + handoff). Each agent starts cold against `design.md` + `state.md` + `brief-phase-b.md` — no carried context across sub-phases. Orchestrator reviews each PR before next sub-phase commissions. See `brief-phase-b.md` "Phase B sub-phase decomposition". |
| **10 explicit guardrails added to brief-phase-b.md** | Made existing CODING_STANDARDS rules unmissable because PR #359 demonstrated the failure modes are real when not enforced upfront: no unsafe casts, no `unknown` without rationale, no type-shadowing, no silent unimplemented placeholders, no inline `eslint-disable`, mandatory `code-reviewer`, integration-branch PR target binding, factory pattern, index.ts barrel-only, Result chaining. Each guardrail tagged to a specific PR #359 failure where applicable. |
| **Model selection: Opus for B-0b probe and B-1 foundation; Sonnet for B-2/B-3/B-4/B-5** | B-0b is a small isolated probe of the new brief shape; Opus for the disciplinary properties. B-1 is the foundation run; Opus where it matters most. After the foundation lands clean, Sonnet against a working foundation for follow-ons. |

---

## PR

Substrate-prep PR: merged (#356, 2026-05-13).
Phase A PR: merged (#357, 2026-05-15).
Phase B brief authoring PR: merged (#358, 2026-05-15).
Phase B first attempt (retired): #359 (closed 2026-05-16, not merged).
Phase B rescope prep PR: this PR.
Phase B sub-phase PRs (incoming): B-0b → B-5.

---

## Resume protocol

Phase A agent: read `brief.md` (binding contract) + `design-brief.md` (binding input — conceptual model is locked, data shapes are proposals) + this `state.md` to resume.
