# State — `ts-agent-memory`

## Timeline
- **Exploration** → `exploration.md` (design-space note; basic-memory lead; substrate pressure-test).
- **Platform design** → `design.md` (invariant core + optional layers; L2/L3 contracts).
- **Consumer ask** (2026-06-25) → `consumer-requirements-personaility.md` + `consumer-assessment-personaility.md`. Commissioned prioritized; promoted to `docs/WORKSTREAMS.md`.
- **Phase A — design-lock** ✅ (2026-06-25) → `design-lock.md`. Implementation-ready interface signatures + knowledge-first build plan.

## Phase A outcomes (locked)
- **JsonEditor merge-patch verdict:** NOT RFC-7386 by default, but **compose with options** — `{ nullAsDelete: true, arrayMergeBehavior: 'replace' }` (verified at `libraries/ts-json/src/packlets/editor/common.ts:57-71`). No extension of the primitive needed.
- **Signature refinements vs `design.md`:** `IMemoryStore.get(kind, entityId)` routed through the codec (+ `getById(scope, id)` for direct access); `IMemoryEnvelope.seq` added (cursor paging); `MemoryScopeKey` widened to multi-segment (`/`); `IBodyConverterRegistry` gains `has`/`getConverter`; `IWritePolicy.applyUpdate(existing, patch: Record<string, unknown>)`.
- **OQ-10/OQ-13:** collapsed — both out of this build (L3); decide at L3 commission.
- **Consumer §8 coverage:** all six criteria mapped to the planned surface (design-lock §9.2).

## Micro-decisions locked by orchestrator (from design-lock §9.1)
1. **Scope encoding:** `defaultMemoryScopeEncoding` — multi-segment, each `/`-component validated portable-filename-safe. Ships in Phase B (so Phase C MTM codec is additive).
2. **Serialization:** YAML frontmatter (`---\n…\n---\n`) via `Yaml.yamlConverter` + markdown body; ~15-line frontmatter splitter, no external dep. Phase B owns `envelopeConverter.ts`.
3. **`seq`:** global per-store-instance counter, incremented atomically inside the write-lock on each successful `put`.

## Phase plan (design-lock §8)
- **Phase B — knowledge slice** (next): packlets `types → converters → store → index → retrieve`, plus `observe` (ring-backed) and `vector` (interface seam only). Ships the `IKnowledgeSearchProvider`-shaped surface; `KnowledgeIdentityCodec` + `KnowledgeLwwPolicy`; 100% tests. Build order in design-lock §8.2.
- **Phase C — memory slice:** experience kinds, attributed edges (cycle-safe), `MemoryCapCullPolicy` (cap-cull + merge-patch), `LtmIdentityCodec`/`MtmIdentityCodec`, `LinkTraversalRetriever`.
- **Fast-follows:** vector (`InMemoryCosineIndex` + `SemanticRetriever` + embed-on-write), temporal (versioned policy + temporal retrievers), observe qualifier-resolver. L2 tools + L3 ingest = separate streams.

## Open for orchestrator (next)
- Decompose Phase B by dependency tier (avoid the single-agent big-bang that drifted on ts-prompt-assist #359): likely B0 scaffold+types+converters → B1 store+index → B2 retrieve+observe+vector-seam, each reviewed before the next.
- Note for Phase B implementer: design-lock §5.3 `MemoryCapCullPolicy.applyUpdate` snippet muddles body-vs-envelope merge target — illustrative only; pin the exact merge surface (mutable **body** fields) in implementation; code-reviewer to confirm.

## Phase B0 — scaffold + types + converters ✅ (2026-06-25)

**Branch:** `claude/ts-agent-memory-b0` (off integration branch `ts-agent-memory`); PR target = `ts-agent-memory`.

**Scope shipped:** Rush project scaffold for `libraries/ts-agent-memory` (`@fgv/ts-agent-memory`) + the `types` and `converters` packlets. No store/index/retrieve/observe/vector (later tiers). No file I/O.

**Files shipped:**
- Scaffold: `rush.json` entry; `package.json` (deps `@fgv/ts-utils`, `@fgv/ts-json-base`, `@fgv/ts-json`, `@fgv/ts-extras` via `rush add`; `sideEffects: false`); `tsconfig.json`, `config/{api-extractor,jest.config,rig}.json`, `eslint.config.js`, `LICENSE`, `README.md` (all mirrored from ts-prompt-assist); `etc/ts-agent-memory.api.md` (api-extractor report, committed).
- `src/packlets/types/`: `ids.ts` (branded `MemoryId`/`EntityId`/`Kind`/`Tag`/`MemoryScopeKey`/`LinkType` + `Convert` constants), `envelope.ts` (`IMemoryEnvelope` incl. `seq`/`temporal?`/`embeddingRef?`, `IEdge`, `ITemporalBlock`, `IProvenance`, `IMemoryRecord<TBody>`), `identityCodec.ts` (`IIdentityCodec` + `IIdentityCodecResult` + `KnowledgeIdentityCodec` + exported `assertPortableFilenameStem`), `writePolicy.ts` (`IWritePolicy`, `AdmissionDecision`, `KnowledgeLwwPolicy`), `index.ts`.
- `src/packlets/converters/`: `bodyConverterRegistry.ts` (`IBodyConverterRegistry` + `BodyConverterRegistry`), `envelopeConverter.ts` (provenance/edge/temporal/envelope converters + `splitFrontmatter`/`joinFrontmatter`/`parseMemoryFile`/`serializeMemoryFile`), `index.ts`.
- `src/index.ts`; tests under `src/test/unit/{types,converters}/` (97 tests).

**Gates:** `rushx build` ✅, `rushx lint` ✅ (0 warnings), `rushx test` ✅ 100% coverage (stmts/branch/funcs/lines), `rushx fixlint` run pre-commit, api-extractor report generated + committed (0 unresolved-link warnings). No `any`; all fallible ops return `Result`; all `unknown`→typed via Converters.

**Deviations / clarifications from design-lock (with rationale):**
1. **`KnowledgeLwwPolicy.applyUpdate` merge surface pinned (resolves the orchestrator's body-vs-envelope flag).** The design-lock §5.3 snippet read mutable fields off the record root (`existing['tags']` etc.) while those fields live on `existing.envelope`. Pinned decision: project each declared mutable field from its **canonical location** (`body` from the body; `tags`/`links`/`provenance`/`embeddingRef` from the envelope) into one record-level JSON view, run the RFC-7386 merge over that view, then rebuild. `body`/`tags`/`links`/`provenance` are required (a patch may not delete them → loud fail); `embeddingRef` is optional (a `null` patch clears it). The orchestrator's note said "mutable **body** fields" referring to the Phase-C `MemoryCapCullPolicy` (whose `mutableMemoryFields` are body-internal); knowledge LWW's design-locked field list is genuinely mixed record-level, so the projection handles both axes.
2. **Two `JsonEditor` instances** in `KnowledgeLwwPolicy` (one default-options clone editor, one merge-patch editor) so the persisted record is never mutated in place and a `null` `embeddingRef` survives the clone step before the patch applies. Merge options per design-lock §5.1: `{ nullAsDelete: true, arrayMergeBehavior: 'replace' }`, rules `[]`.
3. **`applyUpdate` does NOT stamp `updated`/`seq`.** Design-lock §5.3 snippet set `updated: Date.now()` in the policy; pinned decision leaves transaction-time metadata (`updated`/`seq`) store-owned (per §2.5 + §9.1.3) so the policy stays deterministic/pure. The store (B1) stamps them on write.
4. **`assertPortableFilenameStem` exported** from the types packlet (mirrors `ts-prompt-assist/scopeEncoding`'s POSIX-portable + reserved-Windows-device contract) so the Phase-C LTM/MTM codecs reuse the exact escaping contract.
5. **`IIdentityCodec.verifyRoundTrip` idStem-mismatch branch is `c8 ignore`d** in `KnowledgeIdentityCodec` — for an identity codec `encode(decode(stem)).idStem` always equals `stem` when both succeed, so the guard is unreachable here; it exists for the non-identity Phase-C codecs. Justified inline.
6. **`IBodyConverterRegistry.getConverter` added** (design-lock §2.6 prose mentioned it but omitted it from the code block; task scope listed it). Returns `Result<Converter<unknown>>`.
7. **`register`/`registerSchema` return `void`** (per design-lock §2.6 signature) — non-fallible mutators (Map.set semantics), not a `Result<void>` anti-pattern.

**code-reviewer:** run on the final diff — **Approved with advisory items; no P1.** Dispositions:
- **P2-2b (fixed):** `KnowledgeLwwPolicy._rebuild` flipped an originally-absent `embeddingRef` from `undefined`→`null` on any update, which would destabilize the store's content-hash. Changed the absent-in-merged case to restore `undefined` (RFC-7386 delete → absent), so an absent `embeddingRef` round-trips hash-stably; an explicit `null` is preserved only when present. Updated 3 tests (delete→absent, absent-stays-absent, explicit-null-preserved).
- **P2-2e (fixed):** added a boundary test for the 0-suffixed reserved Windows device variants (`COM0`/`LPT0`/`COM9`).
- **P2-2a (dispositioned):** per-instance `JsonEditor` pair (vs. the design-lock module singleton) is intentional — avoids shared mutable state across stores/tests; construction cost is negligible.
- **P2-2c (dispositioned):** `registerSchema` uses `schema.convert` via `Converters.generic`; `convert`/`validate` route through the same logic for schema validators. No change.
- **P2-2d (dispositioned):** `serializeMemoryFile` emits `seq`/`contentHash` into frontmatter — by design; `seq` assignment + hash recomputation are store-owned (B1), flagged for the store stream.
- **P2-2f (dispositioned):** `splitFrontmatter` trims the delimiter line before comparison (lenient `---` match) — conventional frontmatter behavior; the first `---` after the opener legitimately closes the block. No change.
- **P3 items (dispositioned):** `provenanceConverter`/`edgeConverter`/`temporalConverter` are intentionally `@public` (consumers embed provenance/edges in custom body converters); the two acknowledged casts were assessed justified (cycle constraint; structural restoration of already-validated data via the prescribed `as unknown as T` form).

Post-fix gates re-run green: `rushx build`/`lint`/`test` ✅, 100% coverage (99 tests), api-extractor report unchanged (no public-signature change).

## Phase B1 — store + index packlets ✅ (2026-06-25)

**Branch:** `claude/peaceful-archimedes-zntbbq` (harness-assigned; stem was `claude/ts-agent-memory-b1`). Off integration branch `ts-agent-memory` (contains B0). PR target = `ts-agent-memory`, NOT `release`.

**Scope shipped:** the `store` and `index` packlets — the first tier with real I/O (all through FileTree, never `fs`). No `retrieve`/`observe`/`vector` (B2); no LTM/MTM codecs or `MemoryCapCullPolicy` (Phase C); no versioned/temporal write path.

**Files shipped:**
- `src/packlets/store/fileTreeMemoryStore.ts` — `IMemoryStore` + `FileTreeMemoryStore` over `FileTree.IMutableFileTreeDirectoryItem`; `IMemoryStoreListFilter`; `IFileTreeMemoryStoreCreateParams`. `put` sequence: validate body via registry → compute `Crc32Normalizer` content hash over `{ kind, body, links }` → **scope-wide dedup BEFORE policy** → `IWritePolicy.admit` (accept/reject/cull-oldest) → `applyUpdate` on update → store-stamped `created`/`updated`/`seq` (global counter ++ inside the write-lock) → `serializeMemoryFile` → FileTree write → `index.patch('put')`. `get(kind, entityId)` routes through the kind's `IIdentityCodec`; `getById(scope, id)`, `list(filter)`, `delete(kind, entityId)`. `verifyFilenameId` (id↔filename + codec round-trip) on every load. Flat-layout only — `isVersioned: true` fails loudly. Per-instance async write-lock (`_enqueue`) serializes `put`/`delete`. Initial FileTree walk on `create()` rebuilds the index and resumes `seq` past the highest persisted value.
- `src/packlets/store/scopeEncoding.ts` — `defaultMemoryScopeEncoding`: multi-segment (`/`-separated), each component validated portable-filename-safe via B0's `assertPortableFilenameStem` (ships now so Phase-C MTM `conversations/<id>` scope is additive).
- `src/packlets/store/index.ts` — re-exports.
- `src/packlets/index/memoryIndex.ts` — `IMemoryIndex` + `MemoryIndex`; `IIndexedMemoryRecord` (scope+record); `MemoryIndexPatchOp`. Derived `byKind`/`byTag`/`byRecency`/`backlinks` maps; `rebuild(entries)` (store hands it the walk); incremental `patch(op, entry)` (removes prior associations first so a changed kind/tag/link set never strands a stale ref). Composite key `${scope}\0${id}` (NUL collision-proof across scopes).
- `src/packlets/index/index.ts` — re-exports.
- `src/index.ts` — added `export * from './packlets/store'` + `'./packlets/index'`.
- Tests: `src/test/unit/store/{fileTreeMemoryStore,scopeEncoding}.test.ts`, `src/test/unit/index/memoryIndex.test.ts` — in-memory FileTree only (`FileTree.inMemory([], { mutable: true })`), 150 total tests in the package, 100% coverage.

**store↔index acyclic wiring:** the store depends on the `index/` packlet (imports `IMemoryIndex`/`MemoryIndex`/`IIndexedMemoryRecord`); the `index/` packlet imports **only** `types/` and never the store. `rebuild()` consumes the store's read output (the store does the FileTree walk and hands the index a record list), so the index stays a pure in-memory derived view. Confirmed acyclic by the code-reviewer.

**Gates:** `rushx build` ✅, `rushx lint` ✅ (0 warnings), `rushx test` ✅ 100% coverage (stmts/branch/funcs/lines), `rushx fixlint` run pre-commit, api-extractor report regenerated + committed. No `any`; all fallible ops return `Result` (no `Result<void>`); all I/O via FileTree.

**Deviations / clarifications from design-lock (with rationale):**
1. **String-body-only write path (B1 limitation, loud).** `serializeMemoryFile` (B0) takes `body: string`, and the on-disk format is YAML frontmatter + markdown body. The store therefore requires `record.body` to be a string and fails loudly otherwise (`"only string (markdown) bodies are supported"`). Knowledge bodies are markdown strings, so this is faithful to the format; structured (typed→string) body serialization is a fast-follow when a non-string-body kind ships.
2. **Injectable `clock?: () => number` added to `IFileTreeMemoryStoreCreateParams`** (defaults `Date.now`). Not in design-lock §4.4; additive, optional, for deterministic `created`/`updated` in tests. Mirrors the repo's deterministic-test-override pattern.
3. **Update path projects the incoming record's mutable fields into a merge-patch, then runs `policy.applyUpdate`.** `put` takes a full record; on an existing-entity write the store builds the patch from the incoming record's declared `mutableFields` (record-level vocabulary `body`/`tags`/`links`/`provenance`/`embeddingRef`, each read from its canonical location) and calls `applyUpdate`. The persisted body is the incoming `body` string (LWW replaces it). Phase-C policies with body-internal mutable fields will extend the store's `_mutableFieldAccessors` map.
4. **`seq` increments only on an actual write** (first-write or update), not on a dedup no-op — a no-op performs no write and returns the existing record verbatim.
5. **`rebuild(entries)` takes the record list rather than a store-reader callback.** Design-lock §8.1 says "index.rebuild() consumes the store's read API"; the chosen shape keeps the index free of any store import (the store walks the FileTree and passes the records), which is the cleaner way to satisfy the acyclic constraint.

**Design-fidelity note for Phase C / orchestrator (faithful-but-surprising):** `contentHash` covers `{ kind, body, links }` per design-lock §2.5 (NOT `tags`/`provenance`). Combined with scope-wide dedup, two consequences follow that a consumer should know: (a) a **tags-only** re-put of an entity collapses to a dedup no-op (tags are never updated unless body or links also change); (b) **two different entities with identical `{kind, body, links}` in the same scope collapse** — the second `put` returns the first's record and the second file is never written. Both are exactly what the locked hash domain + scope-wide-dedup spec dictate (tested explicitly). Flagging in case Phase C wants the hash domain widened (would be a design-lock amendment, not a silent change).

**Additive B0 exports needed:** none. B0's public surface (`assertPortableFilenameStem`, `serializeMemoryFile`/`parseMemoryFile`, `envelopeConverter`, `KnowledgeIdentityCodec`, `KnowledgeLwwPolicy`, the `Convert` constants, the envelope/record/policy interfaces) was sufficient. No B0 file modified.

**code-reviewer:** run on the final diff — **no P1; Requires-Changes on one P2 (doc/code mismatch).** Dispositions:
- **P2-1 (fixed):** `MemoryIndex._keyOf` — the composite key separator was a raw NUL byte while the source read as a space in some tools (and a raw NUL makes the file grep as binary). Replaced with an explicit `\0` escape sequence and expanded the doc comment to justify NUL as collision-proof (excluded from both scope segments and `MemoryId` by the portable-filename contract). Behavior unchanged (already NUL); now visible/intentional in source.
- **P2-2 (fixed):** `_writeFile` returned `Result<string>` (the raw content the caller already had). Changed to `Result<true>` — honest type; callers discard it anyway.
- **P2-4 (fixed):** the dedup comment said "an identical body … is a no-op"; corrected to "identical `{ kind, body, links }` triple", with a note that tags/provenance are excluded from the hash (the tags-only-no-op consequence above).
- **P3-8 (fixed):** removed the dead `segments.length === 0` arm in `defaultMemoryScopeEncoding` (`''.split('/')` yields `['']`, never `[]`; the `some(empty)` check covers the empty-scope case). Added a clarifying comment.
- **P2-3 / P2-5 (dispositioned — deferred):** the sync `Result<T>` chaining from the `async put` is sound only because the FileTree in-memory/`FsTree` read API is synchronous today. When a genuinely-async adapter (e.g. HTTP-backed) is wired, `_readRecord`/`_writeResolved`/`_evict` move to `AsyncResult` via `thenOnSuccess`. Logged as a fast-follow; not a B1 blocker (the `IMemoryStore` public surface is already `Promise<Result<…>>`, so the migration is internal).
- **P3-6 / P3-7 / P3-10 (dispositioned):** `MemoryIndex.create()` infallible-but-`Result` — kept for family-convention consistency. `_mutableFieldAccessors` static-class-field vs module const — kept co-located with its sole consumer. Test `as string` cast on a branded `MemoryId` for output extraction — standard test carve-out.

Post-fix gates re-run green: `rushx build`/`lint`/`test` ✅, 100% coverage (150 tests), api-extractor report regenerated (new public surface: `FileTreeMemoryStore`/`IMemoryStore`/`IMemoryStoreListFilter`/`IFileTreeMemoryStoreCreateParams`/`defaultMemoryScopeEncoding`/`MemoryIndex`/`IMemoryIndex`/`IIndexedMemoryRecord`/`MemoryIndexPatchOp`).

### Phase B1 — CodeRabbit review round 1 (2026-06-25)

CodeRabbit (CHILL profile) posted 5 actionable + 1 nitpick on PR #497. Dispositions:

- **Fixed — backlinks scope-awareness (Minor/potential-issue + nitpick test).** `MemoryIndex._backlinks` keyed source membership by bare `MemoryId`, inconsistent with the composite-`(scope,id)` keying of `_byKey`; two same-id sources across scopes collapsed and removing one dropped the other's inbound edge. Re-keyed `_backlinks` to `Map<MemoryId, Map<sourceCompositeKey, MemoryId>>` (add/remove by composite key; `backlinks()` returns the source ids). Added the requested same-id-across-scopes backlinks test.
- **Fixed — persist the policy-updated body (Major).** `_buildEnvelope` discarded `applyUpdate`'s returned `body` and always persisted the incoming string; a body-transforming policy would have been bypassed and `contentHash` could describe content other than what was stored. Renamed to `_buildRecord` (returns `IMemoryRecord<string>`): the update path now persists `updated.body` and **recomputes `contentHash` over the policy's actual `{ kind, body, links }`**, with a loud string-guard if a policy returns a non-string body. No behavior change for LWW (merged body === incoming). Added a non-string-policy-body test.
- **Fixed — cull-oldest deletes before persist confirmed (Major).** Reordered the write flow so the new record is built + serialized + written **before** any eviction runs (`_applyEvictions` moved after `_persist`); a cull decision can no longer delete existing records until the replacement is durably on disk. Residual: a failed eviction after a successful write leaves the cap temporarily exceeded (no data loss) rather than losing the old records — the correct trade-off.
- **Fixed — Result matchers in assertions (Major, repo coding-guideline).** Converted the flagged assertion-context `.orThrow()` extractions in the store tests to `toSucceedAndSatisfy(...)` / `toSucceedWith(...)`; genuine arrange-step puts stay as bare `await`. (Layer-1 miss — the `/result-tests` convention; corrected here.)
- **Dispositioned — do not dedup on CRC32 alone (Major).** Declined: CRC32 is the **design-locked** content-hash dedup key (design-lock §2.5/§4.2), so hash-equality-as-dedup is the specified behavior, not an oversight. Adding a canonical-tuple confirmation on a hash hit would introduce a branch (`hash matches, content differs`) that **cannot be covered** without synthesizing a CRC32 collision, breaking the 100%-coverage gate. Widening to a stronger/longer hash (or adding confirmation) is a design-lock amendment for the orchestrator, not a B1 change. Replied on the PR thread with this reasoning.

Post-round gates: `rushx build`/`lint`/`test` ✅, **100% coverage (152 tests)**, api-extractor report unchanged (all changes internal — no public-surface delta).

## Phase B2

**Branch:** `claude/ts-agent-memory-b2` (PR target: `ts-agent-memory`).

**Mission:** the `retrieve` packlet (retrieval interface + v1 non-vector retrievers + loud-degrading semantic retriever), the `observe` packlet (ring-backed audit), and the `vector` interface-seam (no impl). Completes the knowledge-slice substrate the consumer adopts behind `IKnowledgeSearchProvider`.

**Files shipped:**
- `src/packlets/retrieve/retriever.ts` — `IMemoryRetriever`, `IMemoryQuery`, `IMemoryRetrieverCapabilities` (design-lock §6.2 verbatim: `semantic?`/`asOf?` present-but-optional; `supportsSemanticRecall`/`supportsTemporalQuery`/`supportsLinkTraversal`). Plus shared `@public` helpers reused by every retriever: `SEMANTIC_UNWIRED_MESSAGE`, `temporalUnwiredMessage(kind?)`, `NON_SEMANTIC_CAPABILITIES`, `guardRetrieverCapabilities`, `recencyCompare`, `indexedRecordMatchesQuery`, `selectByQuery`, `limitRecords`.
- `src/packlets/retrieve/recencyRetriever.ts`, `tagRetriever.ts`, `structuredFilterRetriever.ts` — over the B1 `IMemoryIndex.entries()` read API; all three report the three capability flags `false`. Tag/structured-filter return an **empty success** (not a failure) when their axis (`tag` / `filter`) is absent, so they contribute nothing to a hybrid rather than loud-failing.
- `src/packlets/retrieve/semanticRetriever.ts` — depends on the `IVectorIndex` seam via an injected `ISemanticBackend { vectorIndex, embedQuery }`. `supportsSemanticRecall = backend !== undefined`. A `query.semantic` request with no backend returns the loud `Result.fail('semantic recall requires a vector index; none configured')` — NEVER silent empty. With a backend: embeds the query → `vectorIndex.query` → resolves hits to records in score order → applies scope/kind/tag/filter + `topK`/`limit`. (The embed-on-write path and the in-package cosine index remain a post-v1 fast-follow.)
- `src/packlets/retrieve/hybridRetriever.ts` — `HybridRetriever.create(retrievers, mergeStrategy)`; capabilities = union of composed. Guards against its OWN (union) capabilities, then **projects the query per child** (strips `semantic`/`topK` for non-semantic children, `asOf` for non-temporal children) so a child never loud-fails on an axis a sibling owns. Child failures propagate (never silently dropped). `IMergeStrategy` injectable; reference impl `ScoreUnionMergeStrategy` (score = number of result sets a record appears in, deduped by `id`, ordered by score then recency).
- `src/packlets/retrieve/index.ts` — re-exports.
- `src/packlets/observe/observer.ts` — `IMemoryObserver` (async `observe` + optional `fireAndForget`), `IMemoryObservationRecord` (`seq` + `timestamp` + `phase` `'read'|'write'|'delete'|'retrieve'` + optional `scope`/`id`/`kind` + `outcome` + optional `error`/`provenance`/`querySnapshot`), `MemoryObservationPhase`, `MemoryObservationOutcome`, `IMemoryObservationQuery`.
- `src/packlets/observe/memoryObservationStore.ts` — `MemoryObservationStore` implementing `IMemoryObserver`, backed by **`Collections.RetainingRingBuffer<IMemoryObservationRecord>`** from `@fgv/ts-utils` (NOT hand-rolled). Schema-aware `query` (`sinceSeq`/`limit` via the ring; `since`/`until`/`scope`/`kind`/`phase`/`outcome` via predicate). `create({ maxRecords? })` validates a positive-integer cap. Mirrors `PromptObservationStore` (pushes pre-assigned `seq`; never mints).
- `src/packlets/observe/index.ts` — re-exports.
- `src/packlets/vector/vectorIndex.ts` — `IVectorIndex` interface ONLY (`add(id, vector): Promise<Result<string>>` returns the `embeddingRef`; `remove(id): Promise<Result<MemoryId>>`; `query(vector, topK): Promise<Result<ReadonlyArray<IVectorQueryHit>>>`) + `IVectorQueryHit { id, score }`. No implementation — the seam `SemanticRetriever` and the B0 `embeddingRef` envelope field hang off.
- `src/packlets/vector/index.ts` — re-exports.
- `src/index.ts` — added `export * from './packlets/retrieve' | './packlets/observe' | './packlets/vector'`.
- Tests: `src/test/unit/retrieve/retrievers.test.ts`, `src/test/unit/observe/memoryObservationStore.test.ts`, `src/test/unit/vector/vectorIndex.test.ts`, `src/test/unit/store/observations.test.ts` — in-memory FileTree only; `@fgv/ts-utils-jest` matchers. **211 tests total in the package, 100% coverage (stmts/branch/funcs/lines).**

**Additive B1 store change (the observer hook):**
- `IFileTreeMemoryStoreCreateParams` gained two optional fields: `observers?: ReadonlyArray<IMemoryObserver>` and `logger?: Logging.ILogger` (defaults to `Logging.NoOpLogger`, used only to log swallowed observer failures).
- `FileTreeMemoryStore` is the **seq authority** for the audit stream: a distinct `_observationSeq` counter (separate from the envelope write `_seq`) mints strictly-increasing `seq` so a single `MemoryObservationStore` fed by the store satisfies the ring's cursor contract. `timestamp` comes from the injected `clock`.
- `get`/`put`/`delete` fire exactly one observation post-op (`'read'`/`'write'`/`'delete'`), on **both success and failure** (failure carries the `error` message). `put` attaches the incoming `provenance`; scope is resolved best-effort via the codec.
- **B1 behavior is unchanged when no observers are supplied:** `_fireObservation` short-circuits on `this._observers.length === 0` (zero clock reads, zero record build, zero fan-out). Tested explicitly (put/get/delete still succeed, produce no records).
- **Observer errors never affect store ops:** `_safeObserve` swallows a failed `Result`, a throw, and a rejection, logging at `warn`. `fireAndForget` observers are dispatched detached (`.catch(() => undefined)`); awaited observers are the default. Mirrors the `ts-prompt-assist` `PromptLibrary._observe`/`_safeObserve` pattern. All four paths tested.
- **No `vectorIndex` param added to the store** (design-lock §4.4 lists one): embed-on-write is the vector fast-follow, out of B2 scope. `SemanticRetriever` takes the `IVectorIndex` directly, not via the store.

**Retrievers do not fire `'retrieve'` observations in B2 (single-authority decision):** the `'retrieve'` phase is in the `MemoryObservationPhase` vocabulary for forward-compat, but no B2 retriever fires one. Rationale: the firing authority is the `seq` authority, and the ring buffer requires strictly-increasing `seq` per push; keeping the store as the sole authority avoids two independent producers interleaving non-monotonic `seq` into one observation store. Wiring retriever-fired observations (with a shared seq authority) is an additive fast-follow.

**Cross-packlet deps (acyclic):** `retrieve` → `types` + `index` + `vector` (semantic retriever consumes the `IVectorIndex` seam — an additive dep over design-lock §8.1's "types + index"; `vector` imports only `types` and never `retrieve`). `observe` → `types` only. `store` → `observe` (the additive hook; `observe` never imports `store`). All confirmed acyclic.

**Gates:** `rushx build` ✅, `rushx lint` ✅ (0 warnings), `rushx test` ✅ 100% coverage (211 tests), `rushx fixlint` run pre-commit, api-extractor report regenerated + committed (+185 lines of new public surface). No `any`; all fallible ops return `Result` (no `Result<void>`); all I/O via FileTree; `RetainingRingBuffer` composed (not hand-rolled).

**Deviations / clarifications from design-lock:**
1. **`SemanticRetriever` requires an `embedQuery` alongside the `IVectorIndex`** (bundled as `ISemanticBackend`). Design-lock §6.3 frames the gate as "an `IVectorIndex` is wired"; in practice a vector index is useless without a way to embed the query text, so the two are wired as one unit and `supportsSemanticRecall` reflects the pair. Honest: when the backend is present, the retriever can actually serve a semantic query; it never claims support it can't deliver.
2. **Shared `@public` retrieve helpers** (`guardRetrieverCapabilities`, `selectByQuery`, `recencyCompare`, etc.) live in `retriever.ts` and are exported. They are genuinely useful to anyone authoring a custom `IMemoryRetriever`, and exporting them keeps the v1 retrievers thin and consistent (one loud-degradation contract, one filter/sort/limit pipeline).
3. **`querySnapshot` on `IMemoryObservationRecord` is typed `Readonly<Record<string, unknown>>`** (an opaque query snapshot) rather than `IMemoryQuery`, to keep the `observe` packlet's dependency surface to `types` only (no `observe` → `retrieve` edge). It is carried verbatim and never interpreted.
4. **Store `logger?` param added** (defaults `NoOpLogger`) for swallowed-observer-failure logging — additive, optional; matches the `ts-prompt-assist` posture.

**code-reviewer:** run on the final B2 diff. **No P1.** Dispositions:
- **P2-1 (fixed) — `HybridRetriever` limit double-application.** `_projectQuery` spread `limit` into every child query; each child pre-truncated via its own `limitRecords`, then the hybrid re-applied `limit` post-merge. With multiple retrievers + a tight limit this starved the merge strategy of candidates (a record both children would surface could be cut before reaching the merge and never scored 2). Fixed: `_projectQuery` now `delete`s `limit` unconditionally (limit is a post-merge concern; the hybrid applies it once after merge); `topK` is retained (it's the semantic vector-query budget, not a record-set cap). Added a multi-retriever + tight-limit regression test that distinguishes the correct `[d,a]` from the buggy `[d,c]`.
- **P2-2 (fixed) — new `ae-unresolved-link` for `Collections.RetainingRingBuffer`.** The `MemoryObservationStore` class doc linked `{@link Collections.RetainingRingBuffer}`, which isn't re-exported from this package → API-Extractor warning. Replaced with an inline `` `RetainingRingBuffer` from `@fgv/ts-utils` `` reference. Also fixed the B2-introduced `{@link Logging.NoOpLogger}` on the new `logger?` param doc (the pre-existing `Logging` links elsewhere are B1 tech debt, untouched). Build now emits zero API-Extractor warnings.
- **P3 (fixed) — `HybridRetriever.retrieve` re-wrapped `fail(guarded.message)`.** Restructured to chain the guard via `thenOnSuccess` (the async-bridge idiom), so a guard failure propagates without re-construction.
- **P2-3 (dispositioned — won't fix, with rationale).** Reviewer suggested `SemanticRetriever.retrieve` call `guardRetrieverCapabilities` for consistency. Declined: the guard catches the semantic-unwired case via `supportsSemanticRecall=false`, but the actual retrieval still needs `this._backend` narrowed to defined — a post-guard `if (this._backend === undefined)` would be **unreachable** when capabilities are consistent, forcing a `c8 ignore` that the repo's coverage-gap policy says to refactor away. The current explicit structure (reachable backend check that IS the semantic guard) is coverage-clean and reuses the same shared `SEMANTIC_UNWIRED_MESSAGE` / `temporalUnwiredMessage` constants, so the loud-degradation messages are identical. The reviewer noted it is "functionally correct in isolation."
- **P3 (dispositioned) — `get` not-found records `outcome: 'success'`, `id: undefined`.** Kept for B2: a not-found read genuinely succeeded (returned `undefined`). A distinct `'not-found'` outcome is a reasonable schema refinement to revisit before the observation schema is declared stable; noted, not changed.
- **P3 (dispositioned) — `IVectorIndex.remove` returns `MemoryId` not `Result<true>`.** Kept: returning the removed id is consistent with `IMemoryStore.delete` (also `Result<MemoryId>`) — the repo precedent — and is more informative than a bare confirmation.
- **P3 (dispositioned) — `as string` cast on branded `MemoryId` in the test `ids()` helper; test fixture seq:0/contentHash:''.** Standard test carve-outs (branded-type casts in tests are explicitly allowed; the fixture deliberately injects a fixed envelope to unit-test observation firing independent of store stamping).

Post-fix gates re-run green: `rushx build`/`lint`/`test` ✅, **100% coverage (211 tests)**, api-extractor report regenerated (no public-surface delta beyond the original B2 additions — the P2-2/P3 fixes are doc-text + internal-structure only).

### Phase B2 — CodeRabbit review round 1 (2026-06-25)

CodeRabbit (CHILL profile) posted 9 actionable + 2 nitpick comments on PR #498. Dispositions:

- **Fixed — link-traversal loud-degradation (3 related findings: `guardRetrieverCapabilities`, `_projectQuery`, RecencyRetriever broad-match).** `IMemoryQuery` exposes `linkedFrom`/`linkedTo`/`hops` and capabilities expose `supportsLinkTraversal`, but no B2 retriever supports link traversal (LinkTraversalRetriever is Phase C). The guard checked only `semantic`/`asOf`, so a link query silently broad-matched on scope/kind/tag — exactly the silent-degradation the §6.1 contract forbids. Added a `LINK_TRAVERSAL_UNWIRED_MESSAGE` constant + a link-axis check in `guardRetrieverCapabilities` (so every retriever that calls the guard — Recency/Tag/StructuredFilter — now loud-fails on an unwired link query), and `HybridRetriever._projectQuery` now strips the link axes for non-link children (matching the semantic/temporal projection). Tests added: guard link loud-fail + supported-pass; Recency link loud-fail; hybrid routes link axes only to the link-capable child. This was a latent bug in the B2 surface, squarely in scope.
- **Fixed — `_safeObserve` logger-throw (Major, stability).** If the injected logger's `warn` threw, an awaited observer could make `get`/`put`/`delete` reject, contradicting the documented non-blocking/swallow-only contract. Wrapped the warn path in `_warnObserverIssue` (its own try/catch). Test added: a logger whose `warn` throws + a failing observer → `put` still succeeds.
- **Fixed — `limitRecords` non-positive limit (Minor).** `limitRecords(records, -1)` returned `slice(0, -1)` (dropped the last item). Now returns `[]` for any `limit <= 0` (public query input). Direct unit tests added (undefined / positive / zero / negative).
- **Fixed — `HybridRetriever` retriever-array snapshot (Major, defensive).** `create()` now copies the caller's `retrievers` array (`[...retrievers]`) before computing+caching capabilities, so a later caller mutation can't make `retrieve()` and `capabilities` diverge.
- **Fixed — weak topK test (Minor).** `FakeVectorIndex` now records the `topK` it received; the test asserts the retriever forwards `query.topK` (was passing even if topK were dropped, since the fake ignored its args and topK > hit count). Added a companion test asserting the default topK of 10.
- **Fixed — `as never` cast in store delete assertion (nitpick).** Replaced `toSucceedWith('doc-a' as never)` with `as unknown as MemoryId` so the branded return-type contract is still enforced.
- **Dispositioned — snapshot observation records (Major, declined).** CodeRabbit wants `MemoryObservationStore.observe`/`query` to deep-copy records to prevent post-hoc mutation of retained audit history. Declined: this mirrors the canonical `PromptObservationStore` exactly (which does NOT snapshot — it pushes the caller-minted record and returns live references), and the store mints a fresh record object per fire that is never mutated afterward. Deep-cloning every record (incl. nested `provenance`/`querySnapshot`) on every observe + every query is a perf cost and a divergence from the established observe-store pattern; if that hardening is wanted it should be applied to `PromptObservationStore` first as a cross-cutting decision, not introduced as a B2-only divergence. (The ast-grep "SQL injection" note on the same lines is a false positive — there is no SQL.)
- **Dispositioned — `IVectorIndex` keyed by `MemoryId` not `(scope, id)` (Major, deferred + surfaced to user).** CodeRabbit notes the vector seam collapses scoped identity, so two records sharing an id across scopes could collide. Correct as a forward-looking concern, but: (a) the seam is **interface-only** with no implementation and no B2 consumer wiring; (b) the collision cannot manifest in B2 (knowledge is single-scope, docIds unique); (c) it's rated "heavy lift" and ripples to the B0 `embeddingRef` field and the Phase-C/fast-follow cosine impl + embed-on-write wiring. Deferred to the vector fast-follow where the implementation lands and the keying decision (composite `(scope,id)` vs dedicated embedding key) belongs; the seam can change additively then since nothing implements it yet. Surfaced to the user as the one architecturally-significant item.
- **Dispositioned — branded `as` casts in `obs()` helper (nitpick).** Skipped: `'x' as Kind`/`as MemoryScopeKey` direct casts in test fixtures are consistent with the existing B0/B1 test style in this package (e.g. `const knowledgeKind: Kind = 'knowledge' as Kind`); switching only the new helper to `as unknown as` would diverge from the package's established fixture convention.

Post-round-1 gates: `rushx build`/`lint`/`test` ✅, **100% coverage (220 tests)**, api-extractor report regenerated (one additive export: `LINK_TRAVERSAL_UNWIRED_MESSAGE`).

## Phase C — memory slice ✅ (2026-06-25)

**Branch:** `claude/pensive-knuth-hgl1mw` (the session's designated dev branch; based on the `ts-agent-memory` integration branch which carries B0+B1+B2). PR target: `ts-agent-memory`. (The brief's suggested stem `claude/ts-agent-memory-c` was superseded by the harness-assigned dev branch.)

**Goal delivered:** experience (memory) kind families on the substrate — LTM/MTM identity codecs, cap-cull + merge-patch write policy, the per-kind dedup-scope amendment, and link-traversal retrieval. The full substrate (knowledge + memory) is now complete.

**Files changed (all additive; B0/B1/B2 existing tests stay green):**
- `src/packlets/types/identityCodec.ts` — **+`LtmIdentityCodec`** (`conversationId` ⇄ `{ scope: 'conversations', idStem: conversationId, isVersioned: false }`) and **+`MtmIdentityCodec`** (`<conversationId>:<turnIndex>` ⇄ `{ scope: 'conversations/<conversationId>', idStem: 'turn-<N>', isVersioned: false }`). Reuses `assertPortableFilenameStem`; relies on B1's multi-segment scope encoding. Turn index preserved verbatim (exact round-trip); the unreachable round-trip-mismatch guard is `c8 ignore`d, mirroring the established `KnowledgeIdentityCodec` pattern.
- `src/packlets/types/writePolicy.ts` — **+`DedupScope`/`DEFAULT_DEDUP_SCOPE`**, **+optional `dedupScope?` on `IWritePolicy`** (default `'entity'`), set **`KnowledgeLwwPolicy.dedupScope = 'content'`** (B1 behavior preserved), **+`MemoryCapCullPolicy` + `IMemoryCapCullPolicyParams`** (cap-cull-oldest admission per design-lock §5.3; RFC-7386 merge-patch over `mutableFields` using the same `JsonEditor` config as `KnowledgeLwwPolicy` — `{ nullAsDelete: true, arrayMergeBehavior: 'replace' }`; `dedupScope: 'entity'`).
- `src/packlets/store/fileTreeMemoryStore.ts` — **amended the put dedup step** (`_writeResolved`) to fetch the policy first and branch on `policy.dedupScope ?? DEFAULT_DEDUP_SCOPE`: `'content'` keeps the B1 scope-wide content dedup; `'entity'` dedups only against the same idStem (same-id identical re-put → no-op; distinct entities with identical content never collapse). Knowledge path unchanged. This is **the additive B1 store/IWritePolicy amendment.**
- `src/packlets/retrieve/linkTraversalRetriever.ts` (new) — bounded, cycle-safe BFS over the B1 index `backlinks` + records' `links[]`; `linkedFrom` = outbound, `linkedTo` = inbound; bounded by `query.hops` (default 1); `capabilities.supportsLinkTraversal = true`. Wired into `retrieve/index.ts`.
- Tests extended in `src/test/unit/{types,store,retrieve}/` (+ new `retrieve/linkTraversalRetriever.test.ts`): LTM/MTM codec round-trips (incl. composite key + `conversations/<id>` scope); cap-cull eviction at `maxRecords` (single + multi victim, by `created` ascending); merge-patch on memory mutable fields; **entity-scoped dedup regression — distinct `turn-5`/`turn-9` with identical `{kind,body,links}` both persist**; same-entity identical re-put no-op; all-three-kinds-in-one-vault + MTM reload round-trip; link-traversal BFS, self-loop + multi-hop cycle termination, cross-kind edge traversal, post-filter/limit, cross-scope id reuse, no-seed loud-fail.

**Gates:** `rushx build` ✅, `rushx lint` ✅ (0 warnings), `rushx test` ✅ **100% coverage (279 tests)**, `rushx fixlint` run pre-commit, api-extractor report regenerated + committed (+~52 lines of new public surface; no `ae-unresolved-link` warnings). No `any`; all fallible ops return `Result` (no `Result<void>`); all I/O via FileTree; `JsonEditor`/`Crc32Normalizer` composed (not hand-rolled).

### Resolved design-lock amendment — per-kind dedup scope

Added optional `dedupScope: 'content' | 'entity'` to `IWritePolicy` (default `'entity'`), implemented exactly as specified in the Phase C brief:
- `'content'` = B1 scope-wide cross-id content dedup. `KnowledgeLwwPolicy` declares it, so its behavior + the existing B1 "dedup is scope-wide: a different entity with identical content collapses" test are unchanged.
- `'entity'` (default) = same-record-id dedup only. Experience kinds (`MemoryCapCullPolicy`) use it. This fixes the flagged collapse bug: two distinct experience entities with byte-identical `{kind,body,links}` no longer collapse into one.

The store amendment is additive and gated by the policy's declared scope; the knowledge code path is untouched.

### Deviations / clarifications from design-lock

1. **Cap-cull's `admit` cohort under the current B1 store wiring (flagged observation, NOT changed).** `MemoryCapCullPolicy.admit` is a correct, unit-proven policy: given an `existing` cohort it accepts under `maxRecords` and returns `cull-oldest` (oldest-by-`created`) at/over it, and the store's pre-existing `_applyEvictions` executes the eviction. **However, B1's store passes `admit` only the *same-idStem* record (a cohort of 0 or 1).** For non-versioned experience kinds each entity is a distinct idStem, so end-to-end cap-cull eviction (a bounded ring of the most-recent-N turns *per conversation scope*) does not trigger through the store today — it would require the store to pass `admit` the scope+kind cohort. That is a change to B1's documented `admit` contract ("current records for this entityId in the scope") which the Phase C brief did NOT authorize (the only specified store change is the dedup-scope branch). **Decision:** implement the policy faithfully and prove eviction at the policy unit level (per the brief's test list, which is policy-level — mirroring the existing `KnowledgeLwwPolicy.admit` unit tests); leave the store `admit` cohort unchanged; surface this as the recommended additive follow-up — *widen the store's `admit` cohort to the scope+kind set for cap-cull kinds (knowledge LWW ignores `existing`, so it is unaffected).* No design-lock §3/§5/§6 signature was ambiguous enough to warrant stopping; this is a B1-wiring limitation, documented rather than guessed-around.
2. **Link-traversal cycle key is a `Set<string>` visited-set, not `Crc32Normalizer`.** design-lock §6.3's table names `Crc32Normalizer.computeHash(canonicalize(visitedSet))` as the cycle key, and the brief permits it *"if you need a structural cycle key."* The link graph is keyed by bare string `MemoryId`s, so a `Set<string>` is the exact, collision-free visited guard — structural hashing would be strictly redundant. Documented in the retriever; no `Crc32Normalizer` dependency added. (The `/value-hashing` reflex was checked and found not to apply here.)
3. **Links are globally-scoped identifiers in this phase.** `IEdge.target` is a bare `MemoryId`, not `(scope, id)`, so traversal resolves a target across all scopes holding that id (mirrors the B1 `backlinks` index). Documented in `_indexById`; scope-qualified link resolution is an additive future change.

### code-reviewer (final diff)

Run on the final diff. **0 P1.** 4 P2 (all fixed), 3 P3 (2 fixed, 1 dispositioned):
- **P2-1 (fixed)** — `{@link DEFAULT_HOPS}` (module-private) produced an `ae-unresolved-link` warning in the committed api.md. Replaced with prose (`default 1 — a single hop`). api.md regenerated, warning gone.
- **P2-2 (fixed)** — `MemoryCapCullPolicy._rebuild` lacked the structural-restoration cast-justification comment that `KnowledgeLwwPolicy._rebuild` carries. Added the mirrored comment.
- **P2-3 (fixed)** — tightened the `_rebuild` docstring to "*declared mutable* required field" to match the guard's actual scope (a required field not declared mutable falls through to `existing.*`).
- **P2-4 (fixed)** — added a design note in `_indexById` stating links are globally-scoped identifiers in this phase and scope-qualified lookup is out of scope (additive later).
- **P3-1 (fixed)** — renamed the misleading `LinkTraversalRetriever wired into a non-link retriever set` describe block to `LINK_TRAVERSAL_UNWIRED_MESSAGE constant` and split the constant assertion from the success-path test.
- **P3-2 (fixed)** — documented verbatim turn-index preservation + the caller-canonicalization footgun (`conv-1:7` vs `conv-1:007`) in the `MtmIdentityCodec` class docstring.
- **P3-3 (dispositioned — kept)** — the `let firstSeq = -1` sentinel assigned inside a matcher callback in the same-entity-no-op store test mirrors the existing B1 `content-hash dedup` test in the same file; kept for package-style consistency.
