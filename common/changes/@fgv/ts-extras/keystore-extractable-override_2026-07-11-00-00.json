{
  "changes": [
    {
      "packageName": "@fgv/ts-extras",
      "comment": "KeyStore.addKeyPair accepts an optional per-call extractable override so a single keypair can differ from the storage backend's default extractability; requesting a value the backend cannot honor now fails loudly.",
      "type": "minor"
    }
  ],
  "packageName": "@fgv/ts-extras"
}
